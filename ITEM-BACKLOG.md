# RideHorizon Delivery Ledger

This file is the canonical delivery ledger. Trello remains intake, Notion holds reusable SOPs, and `PROJECT.md` records only the last verified project state and current gate.

## Preservation-stream adjudication

### RH-050 — Adjudicate the RH-045 preservation streams

- **Type:** Repository recovery and stream disposition.
- **Requested by owner:** 2026-08-17. The owner authorised working through every preserved stream, creating or switching branches as appropriate, and reporting any judgement that cannot be resolved from evidence.
- **Execution branches:** The controller used `codex/rh-050-adjudicate-preserved-streams`. After its PR 15 integration and completion of the five independent stream tasks, `codex/rh-050-close-preservation-adjudication` carries the serialized closeout proof. The second branch is deliberate because the path-disposition record must be integrated before the RH-045 source shelf can be deleted; a final documentation-only follow-up will record the post-deletion topology. Keep `/Users/rob_dev/DocsLocal/motoguide/repo` on `codex/rh-045-preserve-local-state` as a read-only source shelf until that gate passes; use `/Users/rob_dev/DocsLocal/motoguide/integration` as the integration checkout.
- **Outcome:** Every unique path preserved at RH-045 commit `2a1a31e` is either transferred into a coherent tracked task, retained on an explicitly named work-in-progress branch, reconciled and integrated through review, or deliberately retired with rationale. The mixed RH-045 branch is never merged or rebased as a whole.
- **Initial topology — 2026-08-17:** `/Users/rob_dev/DocsLocal/motoguide/repo` was clean at `2a1a31e` on `codex/rh-045-preserve-local-state`; `/Users/rob_dev/DocsLocal/motoguide/integration` was clean at `977374a` on `main`, aligned with `origin/main`. Those were the only local worktrees and the only remote branches were `origin/main` and `origin/codex/rh-045-preserve-local-state`.
- **Preserved delta:** Compared with current `main`, RH-045 changes 55 paths with 8,880 insertions and 68 deletions. The delta includes real tools and app/project changes as well as operational evidence and planning. Historical `Backlog.md`, `PROJECT.md`, `AGENTS.md`, address/geodata and family-framework differences must not be transferred blindly because their current equivalents have since changed or merged.
- **Stream ST-01 — TestFlight automation:** `tools/ios-testflight`, `.ios-testflight.json`, `tools/tests/test-ios-testflight.sh`, fixture executables, TestFlight automation continuation/research and signing-hardening records. Process under RH-019A from clean `main`. Preserve the script/configuration pair together, reconcile the blank App Store Connect app ID and runtime-key custody, apply the secret-management and iOS deployment SOPs before authenticated or release operations, run the 27-fixture harness, and integrate only after security and operational review. Do not run an upload merely to assess the stream.
- **Stream ST-02 — Apple place audit:** `tools/apple-place-audit`, `fixtures/geography/apple-place-audit` and the three 2026-08-05 research records. PR 8 already integrated the rider-facing Apple placemark behaviour, so this stream is supporting audit tooling/evidence rather than required application code. Create a separate decision task from `main`; inspect the retained result data for location/privacy suitability, confirm whether the tool still answers a live verification need, then retain, reduce or retire it explicitly.
- **Stream ST-03 — Beta/UAT material:** headset setup, three ride-UAT protocol variants, field-test evidence and continuation records, distribution research, and the testing policy/strategy/protocol documents. Reconcile against the actual current TestFlight build and present release state before transferring anything. Use RH-002 for owner UAT/headset evidence and RH-019A for distribution-tool material. Preserve only accurate, non-duplicative documents; do not import stale build claims wholesale.
- **Stream ST-04 — App/build identity:** the Settings build-information card in `RideHorizon/ContentView.swift`, build-number/project changes and shared-scheme edits. This is real implementation work associated with RH-037, not documentation residue. Create a dedicated branch from `main`, carry only the intended build-identity behaviour, prove whether any scheme changes are semantic rather than Xcode formatting churn, discard unproved churn, and run proportionate compile/UI verification before a PR.
- **Stream ST-05 — Product planning:** `docs/product/plans/BACKSEAT_GUIDER_PROJECT_SEQUENCE.md` and related name/IP research. Compare with the family-journey framework integrated at `76f35b7` and the family priorities integrated through PR 9. Preserve only genuinely new decisions or evidence in a documentation-only task; retire superseded duplication explicitly.
- **Sequence:** Process ST-01 first because it is the largest reusable operational capability, then ST-02, ST-03, ST-04 and ST-05. Work serially. Each coherent result gets its own tracked item and branch from current `main`; useful but unfinished work gets a clearly named WIP branch; no-longer-useful material is recorded as retired. Do not cherry-pick the mixed RH-045 commits wholesale—transfer only task-owned paths or hunks.
- **Owner judgements:** Ask only where evidence cannot settle a product or risk choice, especially whether to invest in hardening the private TestFlight uploader, whether the Apple audit dataset/tool has ongoing value after PR 8, and whether any BackseatGuider sequence adds direction not already represented by the integrated family framework. Do not ask the owner to resolve mechanical reconciliation or clearly stale duplication.
- **Verification:** For every stream, record source paths, destination work item/branch, retained and retired material, rationale, tests or document checks, PR/commit evidence and remaining owner decisions. Before closing RH-050, prove that all unique RH-045 paths have an explicit disposition, `main` and all retained worktrees are clean, and the RH-045 branch is still remotely reachable or is deleted only after all retained work is independently reachable.
- **Stop condition:** Stop before merging RH-045 wholesale, deleting its last reachable copy, exposing credentials or private ride/location data, performing a TestFlight upload without a separately authorised release task, or making an unresolved product-direction choice for the owner.
- **Controller checkpoint — 2026-08-17:** Branch `codex/rh-050-adjudicate-preserved-streams` was created from verified current `main` at `977374a`. RH-049 was moved from Active to Completed, and its obsolete claim that `.ios-testflight.json` remained uninspected and local-only was corrected against the later `2a1a31e` preservation checkpoint. No preserved stream content has yet been transferred.
- **Closeout evidence — 2026-08-17:** ST-02 to ST-05 integrated through PRs 16–19 at `82e5cb5`, `67c72d1`, `f148845` and `7d50042`. ST-01 remains independently reachable as the clean published RH-019A WIP branch at `ee4686d`. The path-by-path retained, reconciled and retired dispositions are recorded in `docs/project/status/RH-050_CONTINUATION_2026-08-17.md`.
- **Status:** Closeout checkpoint prepared — publish this record, retire the proved-redundant branches/worktrees, restore the canonical checkout to clean `main`, then record the final topology.

### RH-051 — Recover the Apple place-label audit corpus

- **Type:** Verification tooling and dated evidence recovery.
- **Claimed by:** Codex on branch `codex/rh-051-apple-place-audit`.
- **Outcome:** The public-synthetic Apple placemark corpus, its isolated probe and its deterministic report tooling are independently reachable from current `main` as supporting evidence for device/locale comparison and the future MapKit migration.
- **Scope:** Transfer `tools/apple-place-audit`, `fixtures/geography/apple-place-audit` and the three 2026-08-05 research records from RH-045 commit `2a1a31e`; verify that the corpus contains public synthetic coordinates only; run the focused Python tests and dry-run without calling Apple services.
- **Exclusions:** Do not change application code, rerun live Apple geocoding, promote candidates to official boundary fixtures, import personal GPX/location history, or claim the dated macOS responses are current iPhone behaviour.
- **Risk:** Low. Coordinates are deliberately public and synthetic, but the retained Apple responses are dated observations and the underlying `CLGeocoder` API is deprecated on the installed SDK.
- **Acceptance criteria:** The tool/tests/data/research form one coherent reproducible corpus; privacy and evidence-age boundaries remain explicit; focused tests and dry-run pass; no app, Xcode, release or private data changes are included.
- **Privacy and value decision — 2026-08-17:** The tool README expressly limits the corpus to public synthetic coordinates and forbids personal GPX traces or precise personal locations. The retained 19-sample manifest and 27 cached Apple requests are dated service observations, not journey history. The corpus remains useful because it provides a repeatable baseline for the separately deferred physical-iPhone/locale comparison and `CLGeocoder` to MapKit migration; PR 8 integrated app behaviour but did not replace that verification need.
- **Verification — 2026-08-17:** All six focused Python tests passed under `uv` with Python 3.13. The isolated Swift probe compiled against the current `RideHorizon/Address.swift`. The dry-run parsed the complete cache and reported zero pending requests without calling Apple. Regenerating the comparison report produced a byte-for-byte match with the retained fixture. `git diff --check` passed.
- **Review — 2026-08-17:** Independent standards and specification reviews found no implementation or scope defect. The retained README was corrected to describe the completed-cache dry-run accurately and to classify the live runner as an operator-only empirical probe rather than an LLM-safe diagnostic collector.
- **Disposition:** Retain and integrate as supporting verification tooling and dated evidence. No owner judgement is required unless Rob wants to fund the deferred live iPhone/locale or MapKit comparison now.
- **Integration — 2026-08-17:** PR 16 merged at `82e5cb5`; all four GitHub checks passed.
- **Status:** Done — corpus and tooling integrated; live comparison remains separately deferred.

### RH-052 — Reconcile beta UAT, headset and test-system records

- **Type:** Release-evidence and test-documentation reconciliation.
- **Claimed by:** Codex on branch `codex/rh-052-test-system-reconciliation`.
- **Outcome:** Current `main` has one canonical owner ride protocol, durable stationary/headset and test-system guidance, and release evidence that identifies the latest locally verified Internal TestFlight receipt without transferring results between builds.
- **Scope:** Reconcile the RH-045 headset documents, three ride-protocol variants, field evidence, testing framework and continuation records against current `PROJECT.md`, current `TESTFLIGHT_FIELD_TEST_EVIDENCE.md` and the SHA-verified local receipt for `0.12.4 (20260806.221234)`.
- **Exclusions:** Do not contact App Store Connect, run a build/upload, claim exact in-app build confirmation or road evidence, import tester identity or credential metadata, or retain superseded protocol copies merely for history.
- **Risk:** Medium. The documents govern a safety-sensitive physical test, and a stale build identity or transferred result could authorise the wrong binary for external beta.
- **Acceptance criteria:** One build-agnostic canonical ride protocol points to the evidence record for the selected build; retained headset/test documents remain accurate and non-private; `PROJECT.md` and field evidence agree on the latest verified receipt and remaining human gate; obsolete protocol and continuation duplicates have explicit retirement rationale; document links and `git diff --check` pass.
- **Receipt verification — 2026-08-17:** The local protected receipt for `0.12.4 (20260806.221234)` and its SHA-256 companion match. It records `VALID`, `INTERNAL_ONLY`, `IN_BETA_TESTING`, `ready_for_internal_tester`, 182-test unchanged-input evidence and a 203-second deployment. No receipt identity field or credential value was copied into repository documentation, and no live Apple request was made.
- **Retained material:** One build-agnostic `RIDE_UAT_PROTOCOL.md`; the X-COM2 setup/principles documents and their non-private iOS Control Centre screenshot; the seven-document test system; and the later field/UAT evidence reconciled against the current receipt. The historical 2026-08-06 UAT remains explicitly provisional to its earlier build and its results are not transferred to `20260806.221234`.
- **Retired duplication:** The compatibility-only protocol pointer and superseded V2 were not retained; V3 became the single canonical protocol. `FIELD_UAT_CONTINUATION_2026-08-06.md` was not retained because its resolved evidence is now in the canonical field record. The distribution continuation and cloud-permission history were not imported because RH-019A retained the current tool/security boundary without obsolete credential identifiers or operator steps.
- **Verification:** The latest receipt hash matched; document searches find no live `Backlog.md`, `RIDE_UAT_PROTOCOL_V3.md` or current release-gate reference to the superseded candidate outside explicitly historical evidence; the retained screenshot contains only the intended iOS Control Centre UI and no personal content; local Markdown links and `git diff --check` pass.
- **Review — 2026-08-17:** Independent standards and specification reviews found no app/release scope creep. Corrections removed unnecessary home-location context from historical UAT evidence, selected `20260806.221234` in the active run template, relabelled `20260804.0246` as an earlier candidate, and reconciled the live private-beta pack to the same no-evidence-transfer gate.
- **Owner judgement:** Operational, not documentary: Rob must confirm `0.12.4 (20260806.221234)` in RideHorizon Settings while stopped before any physical result is attributed to it, then decide the RH-002 release gate from the recorded stationary/road evidence.
- **Integration — 2026-08-17:** PR 17 merged at `67c72d1`. The documentation-only rebase preserved RH-051 and RH-052 ledger intents; irrelevant restarted app/proxy CI was bypassed under the owner's documentation-only instruction.
- **Status:** Done — documentation integrated; exact-build human evidence remains open under RH-002.

### RH-053 — Show the packaged build identity in Settings

- **Type:** Small iOS observability feature.
- **Claimed by:** Codex on branch `codex/rh-053-settings-build-identity`.
- **Outcome:** A stationary tester can read the app's packaged marketing version and bundle build directly in Settings and report the exact binary under test.
- **Scope:** Transfer only the Settings Build card from RH-045 commit `2a1a31e`, using the existing `AppBuildMetadata` helper. Add focused testability only where needed and run proportionate compile/test verification.
- **Exclusions:** Do not import RH-045 build-number changes, shared-scheme formatting churn, TestFlight tooling, release evidence or unrelated Settings changes; do not archive, upload or alter signing.
- **Risk:** Low. The UI is read-only and derives values from packaged metadata. The main risk is displaying a stale hard-coded value or disrupting the Settings layout.
- **Acceptance criteria:** Settings visibly labels both the packaged version and build; no release identity is duplicated in source; the normal RideHorizon scheme/project semantics are unchanged; relevant automated checks and an unsigned generic-iPhone compile pass.
- **Verification — 2026-08-17:** `git diff --check` passed. A cached unsigned Release build for generic iOS hardware completed with exit status 0 and `** BUILD SUCCEEDED **`. Independent reviews identified that the friendly timestamp truncated build seconds; follow-up commit `e4880fd` now displays the full packaged `CFBundleVersion`, and both reviews passed on recheck. No focused `AppBuildMetadata` test exists; no broad simulator suite was run for this static presentation-only change. No physical iPhone was available in the scheme destinations, so no device install was attempted.
- **Integration — 2026-08-17:** PR 18 merged at `f148845` after all four GitHub checks passed.
- **Status:** Done — exact packaged build identity is integrated in Settings.

### RH-054 — Reconcile family-product sequence and name research

- **Type:** Product-planning and dated research reconciliation.
- **Claimed by:** Codex on branch `codex/rh-054-product-planning-reconciliation`.
- **Outcome:** Current `main` retains the genuinely new product-family delivery decisions and preliminary name evidence from RH-045 without duplicating the integrated family framework or treating a provisional name as adopted.
- **Scope:** Compare the RH-045 BackseatGuider sequence and 2026-08-10 to 2026-08-11 name/IP screen with the integrated family journey framework and PR 9 priority overlay; retain durable delivery boundaries, sequencing and dated evidence; update stale repository authority references.
- **Exclusions:** Do not implement a second app or target, expand the live ledger into a future programme, adopt or register a product name, buy domains, repeat live availability searches, or restore deprecated `Backlog.md` material.
- **Risk:** Low for documentation. The material becomes misleading if dated name signals are presented as current clearance or if a planning sequence is mistaken for implementation authority.
- **Acceptance criteria:** The family framework remains product inspiration; the retained sequence adds only delivery decisions and phase gates; the name screen remains explicitly dated and preliminary; current `ITEM-BACKLOG.md` is the sole live ledger; stale branch, PR and checkout claims are removed; local links and `git diff --check` pass.
- **Disposition — 2026-08-17:** Retain both RH-045 ST-05 source files. The sequence adds delivery boundaries, data/compliance gates, an incremental product-split route and phase exits that the inspirational framework and priority overlay do not supply. Stale branch, PR, checkout and ledger-migration state was removed. The name screen remains useful as dated preliminary evidence; its contradictory GREEN subsection was corrected to the document's final AMBER judgement, and unrelated plugin follow-up was retired.
- **Verification — 2026-08-17:** `git diff --check`, both local cross-document link checks and the secret scan passed. Searches found no stale RH-045 branch/worktree claims, in-flight ledger migration, provisional-name adoption or deprecated live-ledger instruction in the retained documents. Independent standards and task-contract reviews passed after present-tense marketplace claims, RDAP wording and ambiguous clearance language were corrected. No app build or test was run because this stream changes documentation only.
- **Owner judgement:** None is required to retain these documents. Before implementation, Rob must separately choose whether the family passenger experiment outranks current RideHorizon work. Before adopting **Backseat Guider**, commission the documented solicitor-led clearance; before involving an under-13 tester, complete the explicit data/compliance gate.
- **Integration — 2026-08-17:** PR 19 merged at `7d50042`; its applicable documentation/security checks passed and no app build was required.
- **Status:** Done — planning evidence integrated; product activation, name adoption and under-13 testing remain separate owner gates.

## Completed repository maintenance

### RH-049 — Reconcile preservation checkpoint and hygiene ledger

- **Type:** Repository hygiene closeout.
- **Claimed by:** Codex on branch `codex/rh-049-preservation-ledger-reconciliation`.
- **Outcome:** The canonical ledger accurately records the RH-045 TestFlight configuration checkpoint and the completed RH-048 cleanup, leaving only deliberately retained branches and clean worktrees.
- **Scope:** Record commit `2a1a31e` as the non-secret configuration companion to the preserved TestFlight automation; move RH-048 to completed maintenance with its merge evidence; and verify the remaining worktrees, remote branches and dirty paths.
- **Exclusions:** Do not merge the RH-045 recovery state; run TestFlight automation; access credentials; alter application or release behaviour; or merge/delete the unadjudicated family-framework branch.
- **Risk:** Low. Incorrect status recording could conceal an untracked path or imply that preserved work was integrated.
- **Decision impact:** No durable product decision. The future fate of the TestFlight tool and its configuration remains RH-019A release-tool hardening work.
- **Acceptance criteria:** The configuration is reachable remotely on the deliberate recovery branch; the ledger no longer calls it local-only; RH-048 is recorded as merged and complete; `main` is clean/current; and every remaining branch/worktree has a named purpose.
- **Verification:** Confirm clean status for each worktree, fetch/prune `origin`, inspect unmerged remote refs, run `git diff --check`, and verify the merged PR 12 revision.
- **Stop condition:** Stop before any TestFlight operation, secret access, deletion of preserved state, or product-framework disposition.
- **Integration — 2026-08-17:** PR 13 merged at `575b0e4`. This integration-checkout closeout records the required canonical completion after the reviewed documentation change.
- **Status:** Done — ledger reconciliation integrated and verified — Codex, 2026-08-17.

### RH-044 — Migrate the tactical ledger to ITEM-BACKLOG.md

- **Type:** Repository hygiene and control-plane alignment.
- **Claimed by:** Codex on branch `codex/rh-044-item-backlog-migration` during execution.
- **Outcome:** The canonical tactical ledger is named `ITEM-BACKLOG.md`, every live repository reference uses that filename, and historically accurate references retain their original filename with clarification where needed.
- **Scope:** Rename the root tactical ledger without changing existing work-item content; update live Markdown links and operational references; verify no live `Backlog.md` or `BACKLOG.md` authority remains.
- **Exclusions:** Do not implement backlog items, resolve existing work-item ID collisions, alter product or release state, modify the dirty `feat/sub-locality-geodata` checkout, merge existing pull requests, or initialise the strategic Backlog.md project-management system.
- **Risk:** Low. The main risks are a broken relative link, rewriting historical provenance, or creating a competing ledger.
- **Branch:** `codex/rh-044-item-backlog-migration`.
- **Acceptance criteria:** `Backlog.md` is a Git-tracked rename to `ITEM-BACKLOG.md`; live references resolve to the new path; historical references remain accurate; repository-wide search finds no deprecated live authority; `git diff --check` passes; the original dirty checkout is unchanged.
- **Done when:** The focused pull request is integrated, remote `main` contains the renamed ledger and aligned references, and temporary branch/worktree cleanup is complete.
- **Verification — 2026-08-10:** Byte comparison confirms the pre-existing ledger content is unchanged apart from this RH-044 record. `git diff --check` passes. Repository-wide negative search finds no deprecated tactical filename outside this migration record. Updated relative ledger paths resolve to `ITEM-BACKLOG.md`. The original `feat/sub-locality-geodata` checkout retains its pre-existing branch and dirty paths.
- **Integration — 2026-08-17:** PR 10 merged as `303794d`. Local `main` fast-forwarded to `origin/main`; the local task branch was deleted and its clean worktree was retained as the repository's integration checkout. Remote branch deletion was attempted after merge but is blocked by the selected Git CLI profile's GitHub `403`; the merged remote ref remains reachable and is harmless pending an authorised credential-route correction.
- **Status:** DONE — all repository-state changes are integrated and verified; remote-ref retirement is an explicitly recorded credential follow-up, not a delivery blocker.

### RH-045 — Classify and preserve mixed local work

- **Type:** Repository hygiene recovery.
- **Claimed by:** Codex on branch `codex/rh-045-repository-hygiene`.
- **Outcome:** Every material modified or untracked path in the existing `feat/sub-locality-geodata` checkout has a named task boundary, a recovery path and an explicit disposition, without changing app, release or product behaviour.
- **Scope:** Inventory the dirty checkout and current worktrees; classify paths as active, paused, review-ready, local-only, evidence or uncertain; create preservation checkpoints only for clearly coherent non-secret work; and retain intentional worktrees/branches with documented purpose.
- **Exclusions:** Do not merge PR 8 or PR 9; change app, tests, Xcode, proxy, TestFlight or product behaviour; read, commit, move or expose the uninspected local TestFlight configuration; redesign credentials; or delete any unproved-redundant state.
- **Risk:** Medium. The checkout contains mixed release, product, tooling and potentially secret-bearing local configuration work; an incorrect split could lose provenance or publish unsuitable material.
- **Decision impact:** No durable product decision is expected. Stop for Rob if a path has no evidenced owner, an intended split would alter meaning, or publication safety is uncertain.
- **Delivery Risk Cube movement:** Raises production-quality depth only through recoverability and delivery control; functional breadth and implementation fidelity are unchanged.
- **Acceptance criteria:** The feature checkout has no unexplained material state; every retained branch/worktree has a task ID, purpose and recovery path; the private configuration is explicitly local-only and excluded from commits; PR 8 and PR 9 remain unchanged; and `main` remains clean and current.
- **Verification:** Record branch/worktree topology, preservation commit IDs and path classification; verify each checkpoint is clean, its branch is reachable remotely where publication is authorised, and `git diff --check` passes for hygiene-only changes.
- **Stop condition:** Stop before a semantic merge, product/release decision, secret access, or deletion without reachability proof.
- **Classification — 2026-08-17:** `RH-037` build identity is represented by the Settings/Xcode/project-state changes; `RH-002` owner UAT and headset/test-system evidence is represented by the field-test, protocol, headset and testing paths; `RH-019A` release-tool hardening is represented by the distribution tooling and its operational/research records; Apple place-audit tooling/evidence is related to PR 8 but requires a separate task-boundary decision before integration; and BackseatGuider planning is product-direction material awaiting a decision on PR 9 and the product-family route. The modified agent/ledger/index files are supporting control-plane material, not a standalone feature.
- **Preservation checkpoint — 2026-08-17:** Local commit `59f58ce` on `codex/rh-045-preserve-local-state` preserves all non-secret paths exactly as found. The root local TestFlight configuration remains deliberately uninspected, uncommitted and local-only.
- **Preservation configuration update — 2026-08-17:** After explicit owner-authorised inspection, `.ios-testflight.json` was confirmed as non-secret project configuration for the preserved `tools/ios-testflight` automation. Its schema/key names and a secret scan were checked without exposing values; commit `2a1a31e` now preserves it on `origin/codex/rh-045-preserve-local-state`. It is not integrated into `main` and must move only with separately tracked RH-019A release-tool hardening work.
- **Credential recovery — 2026-08-17:** Rob confirmed the designated personal Keychain PAT. A single value-suppressed process-local capability check verified `ADMIN` access to `rjbarbour/MotoGuide`; the standard request-local GitHub CLI credential helper then published both RH-045 recovery branches. No credential value was printed, persisted or copied into repository configuration.
- **Remote recovery — 2026-08-17:** Commit `59f58ce` is reachable at `origin/codex/rh-045-preserve-local-state`; commit `2b750d8` was published at `origin/codex/rh-045-repository-hygiene` before PR 11 integration; and the already merged remote ref `codex/rh-044-item-backlog-migration` was deleted after reachability was proved. The later configuration checkpoint `2a1a31e` adds the inspected, non-secret `.ios-testflight.json` to `origin/codex/rh-045-preserve-local-state` so the preserved TestFlight tool and its required configuration remain recoverable together.
- **Integration — 2026-08-17:** PR 11 merged into `main` at `e48e0f1`. The hygiene controller branch is fully integrated; `59f58ce` remains as the deliberate preservation checkpoint. PR 8 and PR 9 subsequently merged through `origin/main` at `02aca86`.
- **Next action:** Reconcile the exact TestFlight build identity across `PROJECT.md`, `docs/operations/testflight/TESTFLIGHT_FIELD_TEST_EVIDENCE.md` and the active UAT protocol, then split the preserved streams into separately tracked work before integration.
- **Status:** Done — recovery state is integrated and the remaining checkpoint is deliberately retained — Codex, 2026-08-17.

### RH-048 — Complete post-merge repository cleanup

- **Type:** Repository hygiene closeout.
- **Claimed by:** Codex on branch `codex/rh-048-post-merge-cleanup`.
- **Outcome:** `main` has an accurate post-merge ledger record, redundant merged branches and worktrees are retired only after reachability is proved, and the remaining recovery and product-framework branches have explicit dispositions.
- **Scope:** Record PR 8 and PR 9 integration; retire the merged `feat/sub-locality-geodata`, `agent/family-trip-validation-priorities` and `codex/rh-045-repository-hygiene` branches/worktrees after verification; retain the RH-045 preservation checkpoint; and inspect and classify the unmerged family-framework branch.
- **Exclusions:** Do not read, stage, move or publish `/Users/rob_dev/DocsLocal/motoguide/repo/.ios-testflight.json`; split or integrate the preserved mixed state; implement RH-046 or RH-047; alter application, proxy, TestFlight or product behaviour; or merge/delete the family-framework branch without a product decision.
- **Risk:** Low for verified merged-ref retirement; medium for any unproven preservation or product-framework disposition.
- **Verification — 2026-08-17:** PR 9 merged at `91f708b`; PR 8 merged at `02aca86`. The three merged source refs proved ancestors of `origin/main` before their clean worktrees, local branches and remote branches were retired. `origin/codex/rh-045-preserve-local-state` was deliberately retained.
- **Integration — 2026-08-17:** PR 12 merged at `3cf403c`; its review comment was resolved, its source worktree and branch were retired, and the integration checkout fast-forwarded cleanly.
- **Post-closeout — 2026-08-17:** The previously retained family-framework material was integrated into `main` at `76f35b7`; its source branch was then proved reachable and retired.
- **Status:** Done — post-merge cleanup integrated and verified — Codex, 2026-08-17.

## Completed release increments

### RH-023 — Publish the support email correction

- **Type:** Release or operations.
- **Claimed by:** Codex in the shared checkout.
- **Outcome:** The public RideHorizon support page displays and links to `support@digitalmercenaries.ai` instead of the privacy address.
- **User/operator:** Testers and App Store reviewers can contact the correct support mailbox.
- **Boundaries:** Change only the support-page email address and publish the existing Cloudflare Pages site; do not change page design, routing, DNS, proxy behaviour or other contact details.
- **Risk:** Low and reversible through the retained previous Cloudflare Pages deployment. Deployment is a production external effect; use only the existing provider-managed Wrangler authentication without reading or exposing credential values.
- **Decision impact:** No durable decision or ADR required; this is a reversible copy correction.
- **Delivery Risk Cube movement:** Hold functional breadth and implementation fidelity fixed; correct public-release operability.
- **Evidence:** Confirm both visible text and `mailto:` target in source, run the existing site tests, deploy successfully, then fetch the canonical `/support` page and confirm the new address is present and the old privacy address is absent.
- **Stop condition:** Stop before deployment if tests fail, Wrangler authentication is unavailable, or the pending deployment contains unrelated public-site changes.
- **Evidence — 2026-08-04:** Both the visible address and `mailto:` target use `support@digitalmercenaries.ai`; all seven edge-router tests passed; Cloudflare Pages deployment `https://8511db66.ridehorizon-edge.pages.dev` completed; its `/support` page and canonical `https://ridehorizon.digitalmercenaries.ai/support` both contain the corrected address and omit the former privacy address; the canonical page returns HTTP 200 with `text/html; charset=utf-8` and the expected security headers.
- **Gate:** **DONE.** The production correction is verified; no follow-up implementation is required.

## Completed product-definition increments

### RH-032 — Clarify the geographic-awareness capability ladder and guide references

- **Type:** Decision and product-definition increment.
- **Claimed by:** Codex in the shared checkout.
- **Outcome:** The canonical milestone plan distinguishes visual geographic awareness, passive boundary/place awareness, a passive contextual tour guide and an interactive contextual tour guide, with the actual 2026-08-04 behaviour and principal functional delta for each level.
- **User/operator:** Product shaping and future implementation agents can preserve the core location-awareness value while adding richer optional guide behaviour deliberately.
- **Boundaries:** Documentation only. Do not implement microphone listening, new location sources, landscape detection, ride memory, narration, POI discovery or navigation handoff.
- **Risk:** Low and reversible. The main risk is creating a competing roadmap, so the ladder lives in `MILESTONES.md` and the two new documents are explicitly non-authorising reference models.
- **Decision impact:** Refines existing product intent without changing system boundaries or requiring an ADR.
- **Delivery Risk Cube movement:** Increases product-definition clarity; implementation fidelity and production-quality depth remain unchanged.
- **Evidence — 2026-08-04:** Reconciled against `LocationManager`, `AnnouncementPolicy`, `PlaceFactRequest` and `ContentView`; added the capability ladder to `MILESTONES.md`; added linked human passive-guide and interactive-guide references; updated `AGENTS.md`, the Location-screen design and the documentation index; checked local Markdown links and reviewed the diff without changing runtime code. An independent review identified and then confirmed correction of the audio-first purpose conflict, the current moving-map-lock overclaim and the passive-versus-interactive "always-on" ambiguity; planned speed-gated map interaction remains explicitly future M6 work.
- **Gate:** **DONE.** Later implementation remains subject to the current private-TestFlight gate and separately shaped work items.

## Paused at milestone health gate

### RH-001 — Harden the private TestFlight release candidate

- **Type:** Release and health increment.
- **Claimed by:** Codex on branch `codex/rh-001-testflight-hardening`.
- **Outcome:** The final iPhone candidate is safe and privacy-consistent for Apple review, passes deterministic checks, validates as a signed distribution archive, uploads to App Store Connect, and remains compatible with the live Fly proxy.
- **User/operator:** Rob can select the processed build for internal TestFlight testing without changing credentials or entering a tester token.
- **Included:** Reconcile the six failing iOS unit tests; prevent proxy-session provisioning before the rider enables optional AI features; add an explicit stationary-setup and non-interaction safety statement to onboarding; keep automatic restricted sessions and Apple Voice fallback working; update matching review metadata; build and install on the physical iPhone; independently review; archive, validate and upload when all local gates pass.
- **Non-goals:** Full App Attest enforcement, public App Store listing assets, payments, subscriptions, Android/iPad support, route guidance, or wider beta recruitment.
- **Dependencies:** `docs/operations/testflight/TESTFLIGHT_PRIVATE_BETA_PACK.md`, `docs/architecture/plans/APP_ATTEST_PROXY_ACCESS_PLAN.md`, `docs/operations/privacy/PRIVACY_AUDIT_2026-07-18.md`, `docs/operations/app-store/APP_STORE_CONNECT_TEST_INFORMATION.md`, the live Fly proxy, Cloudflare privacy URL, Apple signing, and App Store Connect access.
- **Risk:** Medium. The changes are small and reversible, but consent timing affects privacy behaviour and archive upload is an external side effect. No secret value may enter source, logs, commands, chat or Git.
- **Decision impact:** Record the consent-timing decision here and in the existing proxy-access plan. No ADR is required because on-demand provisioning preserves the existing API and trust boundaries and is easily reversible.
- **Delivery Risk Cube movement:** Hold functional breadth fixed; raise implementation fidelity from local device candidate to signed uploaded build; raise production-quality depth for privacy, safety, reliability and release operability.
- **Behavioural evidence:** A clean launch makes no RideHorizon proxy request before the AI choice; declining AI keeps facts local and uses Apple Voice; granting AI provisions automatically on first proxy-backed request; Test Mode starts at its first sample point; Premium Voice failure can use Apple Voice fallback; onboarding tells the rider to set up while stopped and not interact while moving.
- **Quality evidence:** No secret exposure; privacy manifest and policy remain consistent; Release diagnostics remain value-free; iPhone-only packaging remains intact; the live health, fact and speech paths remain available.
- **Deterministic verification:** `xcodebuild test -project RideHorizon.xcodeproj -scheme RideHorizon -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath DerivedData-TestFlight-Hardening -only-testing:RideHorizonTests` — expected result: `** TEST SUCCEEDED **` with zero failures.
- **Deterministic verification:** `./gradlew test` from `fact-proxy` — expected result: `BUILD SUCCESSFUL`.
- **Deterministic verification:** `xcodebuild build -project RideHorizon.xcodeproj -scheme RideHorizon -destination 'platform=iOS,id=00008150-000C70883E87401C' -derivedDataPath DerivedData-TestFlight-Hardening -allowProvisioningUpdates` — expected result: `** BUILD SUCCEEDED **`.
- **Independent evaluation:** A fresh review context checks the diff against this contract, Apple review risks, privacy boundaries and maintainability. Rob performs the final human TestFlight interaction check.
- **Stop condition:** Stop before upload if tests, signed validation, privacy evidence, live proxy checks or independent review fail. Stop and ask Rob if Apple authentication or a Keychain modal requires human interaction, or if implementation would change the approved proxy security boundary.
- **Gate:** Continue only to RH-002 after Apple reports the uploaded build processed successfully.
- **Evidence — 2026-07-31:** Full `RideHorizonTests` suite passed after final review fixes; fact-proxy Gradle tests passed; public health and privacy routes returned HTTP 200; automatic fallback session issuance, fact generation and speech generation returned HTTP 200; speech response was a valid 44,765-byte `audio/mpeg`; independent review found no blocking code issue; Release archive `0.12.3 (20260731.1857)` passed Xcode archive and local store validation.
- **Blocked gate — 2026-07-31:** The physical iPhone is unavailable to CoreDevice. The earlier `20260731.1942` archive upload stopped before transfer with `exportArchive Failed to Use Accounts`, and the reviewed reliability increment advances the current build to `20260731.2020`. Rob must reconnect/unlock the iPhone and refresh the Apple ID session in Xcode before this exact build can be archived, installed and uploaded.
- **Additional simulator evidence — 2026-07-31:** Replaced the skipped UI-test placeholders with a three-test clean-install suite. Before the recorded suite, the iPhone 16e simulator app was uninstalled and its permissions were reset. The suite verifies stationary-use copy, bounded layout, static Back-button position, absence of token/invite/API-key fields, all onboarding pages, privacy details and policy link, on-device-only consent, the real location permission prompt and main-screen launch. Visual inspection found and fixed horizontally clipped onboarding copy. A fresh iPhone 16e / iOS 26.3.1 run then exposed fixed-height landscape content; onboarding content is now adaptive and scrollable while navigation controls remain fixed. The landscape test waits for a width-greater-than-height app frame, verifies page-one scrolling and the longer AI-consent page, and reaches the privacy notice. Compact portrait and correctly oriented landscape screenshots are retained with the passing test results. Candidates `20260731.1857` and `20260731.1930` are superseded by `20260731.1942`.
- **Replacement archive — 2026-07-31:** Release archive `0.12.3 (20260731.1942)` passed Xcode's store validation. It is arm64/iPhone-only, contains the valid privacy manifest, declares no non-exempt encryption, has only location/audio background modes, has an opaque app icon, has matching binary/dSYM UUIDs, passes strict code-signature verification and exposes no credential-entry UI.
- **Observed infrastructure risk — 2026-07-31:** Fly is configured with `min_machines_running = 0`. A cold-start health request exceeded 20 seconds before the machine started; the next request returned HTTP 200. The app's health timeout is 6 seconds and fact/speech timeouts are 15 seconds, so the first optional AI request can fall back to Apple Voice after an idle period unless one machine is kept warm during the TestFlight window.
- **Reliability increment authorised — 2026-07-31:** Keep one Fly proxy machine warm for the private-beta window and replace the conflicting 15-second client limits with one coherent rider-network policy. Fact and Premium Voice operations have a 60-second wall-clock ceiling; each proxy attempt may wait up to 35 seconds so the proxy's 30-second OpenAI/ElevenLabs deadline can complete; transient connection failures and HTTP 408/502/503/504 may retry after 3 and 10 seconds while permanent 4xx responses do not retry. A structured Premium Voice failure retries only the generic transport/provider category, not authentication, account-capacity or throttling categories. Automatic session provisioning uses the same transient-failure classification within a shorter 30-second ceiling. Cancellation must stop retry/backoff promptly, and a superseded fact or speech request must never produce delayed audio or Apple fallback. Existing 401 session invalidation/reprovisioning remains compatible with the deployed proxy.
- **Reliability acceptance evidence — 2026-07-31:** Focused tests prove recovery after transient network and gateway failures, no retry for permanent/capacity failures, cancellation during backoff, prompt wall-clock expiry even when session provisioning ignores cancellation, preservation across street-only geocoder churn, boundary-priority preservation and suppression of superseded Premium Voice fallback. The complete iOS unit target, generic iPhone build, proxy Gradle suite and OpenAPI parse pass. Two independent reviews found no remaining blocker in commit `1665bac`. Fly production version 38 preserves `https://motoguide-fact-proxy.fly.dev`, sets `min_machines_running = 1`, and reached `started`; live health returned `ok`, automatic restricted session issuance succeeded, facts returned HTTP 200 `application/json` with 259 bytes, and ElevenLabs returned HTTP 200 `audio/mpeg` with 34,316 bytes. The unavailable physical iPhone remains the only unrun device-install check for this increment.
- **Live-location default correction authorised — 2026-08-02:** A fresh install must start with Test Mode off so the release candidate processes real location updates. An explicit saved Test Mode choice must continue to persist. Acceptance requires focused tests for both behaviours, the complete iOS unit target, and a current physical-iPhone build/install when the device is available.
- **Live-location default evidence — 2026-08-02:** Candidate `0.12.3 (20260802.2254)` defaults Test Mode off only when no saved choice exists and retains an explicit saved choice. Both focused regression tests and the complete `RideHorizonTests` target passed on the unlocked physical iPhone: 115 tests, zero failures. The complete app and test targets compile, the unsigned generic-iPhone Release build passes, and the normal signed candidate was installed and launched successfully on `Robert’s iPhone 17`.
- **Milestone health gate — 2026-08-02: REVISE.** Source review confirms that tracking starts automatically after onboarding, enables background location under Always permission, disables automatic location pausing and has no inactivity end condition. It also confirms that the playback audio session is activated during `LocationManager` initialisation and is not deactivated after announcements; the music-interruption option therefore can remain in effect beyond speech. These are safety and privacy blockers for external testers. Do not archive or upload this candidate. Complete RH-003 and RH-004, pass their deterministic and pre-road physical checks, then upload the replacement for exact-binary field testing through Internal TestFlight.
- **Replacement candidate evidence — 2026-08-03:** Commit `aba7138` produced Release archive `0.12.3 (20260803.0032)`. Xcode archive and its store-oriented bundle validation succeeded. The archive is arm64/iPhone-only, contains `PrivacyInfo.xcprivacy`, declares no non-exempt encryption, has only location/audio background modes, includes the canonical privacy-policy URL, has matching binary/dSYM UUIDs and passes strict code-signature verification. Distribution export stopped before export or upload because Xcode has only an Apple Development certificate; its signed-in Digital Mercenaries Ltd team offers creation of the missing Apple Distribution certificate. Creation remains human-gated because it adds a private signing credential to macOS Keychain. The five stationary checks also remain required before upload.
- **Distribution validation evidence — 2026-08-03:** After Rob authorised continued release preparation, Xcode used Digital Mercenaries Ltd's cloud-managed Apple Distribution certificate and the App Store provisioning profile for `ai.digitalmercenaries.ridehorizon`; no certificate value or private key was exposed. The signed export reports `beta-reports-active=true` and `get-task-allow=false`. Apple completed server-side validation successfully at `2026-08-03T01:44:00+01:00`. The retained `RideHorizon.ipa` passes strict code-signature verification, remains iPhone-only, contains `PrivacyInfo.xcprivacy`, declares no non-exempt encryption and has SHA-256 `d435b14b2c6a7080fe82ad4f109e3da4e076072d3152000f50632eeb27157620`. No build was uploaded. The five stationary checks remain the only pre-upload safety gate.
- **Stationary evidence — 2026-08-03: REVISE.** Rob confirmed that the app opens idle, Start ride begins tracking, End ride stops tracking and audio activity, and both Apple Voice and Premium Voice previews play. The map intentionally remains visible after End ride, so the idle status must remain unambiguous. `TF-AUDIO-02` failed: while YouTube Music was playing, RideHorizon produced no spoken announcement and advancing Test Mode across the England boundary did not visibly retrieve or speak the next phrase. Do not upload this candidate. Reproduce and correct the competing-audio event chain, then repeat all five stationary checks.
- **Pre-upload UI correction — 2026-08-03:** Remove the duplicated RideHorizon brand block between Start ride and Log; retain the compact top-left brand only. Add a glanceable, non-distracting announcement-pipeline status so a stationary observer can distinguish place/content retrieval from Premium Voice audio generation and understand what phrase is pending. Do not expose generated text while moving or encourage screen interaction.
- **Competing-audio correction — 2026-08-03:** The failure was reproduced at the real `LocationManager` audio seam: `secondaryAudioShouldBeSilencedHint` remained true while music played, so RideHorizon deferred the announcement indefinitely. The temporary three-second fallback timer was subsequently rejected and removed from all modes. A preflight hint no longer delays speech; only a genuine interruption or secondary-audio begin notification can pause active speech, and the corresponding end/resumable event restarts it immediately. The centre toolbar reports Finding fact, Phrase ready, Waiting for audio, Creating voice or Speaking and otherwise reports Ride stopped/Ride active; the duplicate centre brand was removed. Physical-iPhone regression tests and the complete `RideHorizonTests` suite are required before the repeated stationary checks; do not upload until `TF-AUDIO-02` passes perceptually.
- **Release-candidate evidence — 2026-08-04:** Candidate `0.12.4 (20260804.0246)` passed all 177 iOS tests on the physical iPhone, the fact-proxy suite, and live Fly health, automatic-session, fact and Premium Voice checks. The exact iPhone-only Release archive passed Apple Distribution signing, Apple server-side validation, strict IPA signature and entitlement checks, privacy-manifest validation and binary/dSYM UUID matching; it contains no test or calibration artefacts. The same version/build installs and launches on the target iPhone. Xcode reported `Upload succeeded` and `Uploaded package is processing.` App Store Connect processing and exact-Internal-TestFlight human evidence remain open; do not invite external testers yet.
- **Completion gate — 2026-08-04: CONTINUE.** App Store Connect reports upload `0.12.4 (20260804.0246)` **Complete** and the build **Ready to Submit**. RH-001 is complete. Continue to RH-002 for internal-group assignment, installation of the exact TestFlight binary and mandatory human evidence; do not invite external testers yet.

## Verification

### RH-003 — Bound background work to an explicit ride session

- **Type:** Safety, privacy and lifecycle increment.
- **Outcome:** RideHorizon performs continuous/background location and announcement work only within a rider-started session, and reliably releases it after manual or confirmed inactivity end.
- **User/operator:** A rider can understand when RideHorizon is active, start it before setting off, and end it without relying on force-quit or iOS intervention.
- **Boundaries:** Add Idle, Riding and Awaiting Confirmation states; use Start ride and End ride actions; keep app launch idle; detect inactivity using the accepted-sample algorithm in `docs/operations/testflight/TESTFLIGHT_FIELD_TEST_EVIDENCE.md`; alert or locally notify the rider; allow a two-minute confirmation grace; automatically end when unanswered; stop location, network, timers and speech on end. Request notification permission contextually at the first Start ride; if permission is denied, show the alert only while foregrounded and still auto-end after the grace period in the background. Do not programmatically terminate the app.
- **Non-goals:** Route navigation, automatic trip recognition, motion-activity classification, CarPlay or public-beta polish.
- **Dependencies:** The accepted lifecycle in `docs/operations/testflight/TESTFLIGHT_FIELD_TEST_EVIDENCE.md`; existing location permission wording; physical iPhone access for background evidence.
- **Risk:** High. False negatives can leave privacy-sensitive background work active; false positives can stop useful announcements. The prompt must not require interaction while moving.
- **Decision impact:** Accepted by Rob on 2026-08-02: adopt **Start ride** and **End ride** rather than automatic start or “guiding”, with a 10-minute inactivity window, 50-metre confidence-adjusted displacement threshold and two-minute confirmation grace.
- **Delivery Risk Cube movement:** Hold breadth fixed; increase implementation fidelity through a bounded state machine and production-quality depth through privacy, battery and lifecycle controls.
- **Behavioural evidence:** Automated scenarios prove the session state machine and cleanup paths; `TF-SESSION-01` to `TF-SESSION-03` pass on the physical development candidate before upload. Retain `TF-IDLE-01` to `TF-IDLE-05` and `TF-END-01` to `TF-END-02` as release evidence for the exact Internal TestFlight binary under RH-002.
- **Quality evidence:** Unit tests cover state transitions, filtered displacement, notification-independent grace expiry, expired-prompt rejection, movement recovery, stale-geocode rejection, Test Mode restart and idempotent cleanup. A physical iPhone proves location stops after both end paths.
- **Deterministic verification:** Complete `RideHorizonTests` target and signed physical-iPhone build pass. Exact commands must be recorded with the implementation evidence.
- **Independent evaluation:** Fresh review checks state-machine correctness, permissions, concurrency, cleanup and rider-safe copy. Rob performs the stationary and road evidence.
- **Stop condition:** Stop before release if background work survives End ride or timeout, or if the prompt encourages interaction while moving.
- **Gate:** Continue to RH-004 only after the session lifecycle is deterministic and the pre-road physical checks pass. Prove the full background behaviour later on the exact Internal TestFlight binary.
- **Implementation evidence — 2026-08-03:** Candidate `0.12.3 (20260803.0032)` adds a deterministic Idle/Riding/Awaiting Confirmation state machine, confidence-adjusted movement filtering, contextual local-notification request, two-minute grace and idempotent manual/automatic cleanup. Review hardening rejects expired continuation, invalidates place-lookup generations at End ride and at the inactivity prompt, ignores queued location callbacks after End ride and resets Test Mode per session. The physical iPhone unit target passed 144 tests with zero failures. The signed app built, installed and launched. `TF-SESSION-01` to `TF-SESSION-03` remain a human stationary gate before upload.
- **Manual-test acceleration — 2026-08-04:** Add one Debug-only, default-off Developer toggle that applies 30 seconds of inactivity plus a 30-second confirmation grace to the next ride. Keep the Release/TestFlight timing fixed at 10 minutes plus two minutes. This changes only lifecycle parameters so the physical locked-phone path can be exercised quickly; it does not redefine the accepted production behaviour.
- **Manual-test acceleration evidence — 2026-08-04:** All nine focused lifecycle tests passed, including the custom 30-second/30-second transition path. The signed Debug physical-device build and the unsigned generic Release build succeeded. Debug `0.12.3 (20260803.2218)` installed and launched on Robert’s iPhone 17; the short-timeout control is excluded from Release by compilation condition.

### RH-004 — Make audio coexistence observable and bounded

- **Type:** Safety, diagnosis and audio-lifecycle increment.
- **Increment 1 status — 2026-08-03: VERIFY.** Candidate `0.12.3 (20260803.2218)` implements the accepted timer-free, event-driven policy and is installed at the YouTube Music perceptual gate. Preflight audio hints do not delay an announcement; genuine interruption and secondary-audio begin notifications pause active speech until every corresponding end/resumable event, then restart immediately. Retain conservative per-announcement Premium Voice peak normalisation, correlated place-lookup timing and one-time Road plus Names Only defaults for the Debug campaign. iOS owns third-party resumption after notification, so do not claim or fake a cross-app fade. Google Maps tuning remains explicitly excluded until this gate passes.
- **Focused validation plan:** `docs/architecture/plans/AUDIO_INTEROPERABILITY_VALIDATION_PLAN.md` owns the audio requirements, platform assumptions, diagnostic contract and staged YouTube Music → Google Maps evidence design. This ledger remains the status authority.
- **Speech calibration probe:** `docs/architecture/specs/SPEECH_INTELLIGIBILITY_CALIBRATION_SPEC.md` owns the delta audit, fixed Premium Voice fixtures, internal-only calibration lab and human profile-selection gate. It does not authorise automatic promotion of experimental processing.
- **Speech calibration probe status — 2026-08-03: VERIFY.** Internal candidate `0.12.3 (20260803.2300)` is installed and launched on the physical iPhone at the human calibration gate. The current production profile remains unchanged. Three fixed Premium Voice fixtures were generated once through the live restricted-session Fly proxy and bundled with SHA-256/provenance metadata; runtime playback is local-only. One reusable processor now serves production preparation and calibration profiles. The internal lab reloads raw fixture bytes for every A/B play, uses the existing Apple Voice and audio-session lifecycle, refuses active rides, persists experimental profiles separately and records privacy-safe calibration outcomes. The Calibration target passed 185 physical-device tests; the normal Debug target passed 173. Offline EBU R128 inspection of all three fixtures rendered with representative (+6 dB/Light/+2 dB) and extreme (+12 dB/Strong/+4 dB) candidates measured true peaks from −2.7 to −2.1 dBFS. The unsigned Release build passed, with file and binary-string inspection finding no fixture, manifest, lab label, navigation entry or internal persistence key. Stop for Rob's stationary phone/headset comparison; do not promote a profile, tune Google Maps, change the proxy contract, archive or upload.
- **Speech calibration follow-up — 2026-08-03: VERIFY.** Internal candidate `0.12.3 (20260803.2345)` reports system output volume dynamically, uses explicit 6 dB choices for output (0…24 dB) and presence (0…18 dB), and states that compression affects Candidate B only. Focused rendering proves Strong changes fixture samples. An explicit internal-only switch persists the current Candidate B profile for normal Premium Voice announcements after leaving the lab; Candidate B playback and Save do not enable it. The profile cannot be changed or disabled while a ride is active, and its active state is shown on the main screen. The Calibration target passed 189 physical-device tests and normal Debug passed 173. Offline representative (+12/Light/+6 dB) and maximum (+24/Strong/+18 dB) renders measured −2.9 to −2.1 dBFS true peak. The unsigned Release build passed and contains no calibration resource, UI label or override persistence key. The production profile remains unchanged. Return to Rob for stationary adjustment and then the authorised road evaluation; do not archive or upload.
- **Speech calibration follow-up verification commands — 2026-08-03:** Calibration tests: `xcodebuild test -quiet -project /Users/rob_dev/DocsLocal/motoguide/repo/RideHorizon.xcodeproj -scheme RideHorizonCalibration -destination 'platform=iOS,id=00008150-000C70883E87401C' -derivedDataPath /Users/rob_dev/DocsLocal/motoguide/repo/DerivedData-SpeechCalibration-Followup -only-testing:RideHorizonTests` (expected: exit 0, all tests pass). Normal tests: `xcodebuild test -quiet -project /Users/rob_dev/DocsLocal/motoguide/repo/RideHorizon.xcodeproj -scheme RideHorizon -destination 'platform=iOS,id=00008150-000C70883E87401C' -derivedDataPath /Users/rob_dev/DocsLocal/motoguide/repo/DerivedData-SpeechCalibration-Followup-Normal -only-testing:RideHorizonTests` (expected: exit 0, all tests pass). Release build: `xcodebuild build -quiet -project /Users/rob_dev/DocsLocal/motoguide/repo/RideHorizon.xcodeproj -scheme RideHorizon -configuration Release -destination 'generic/platform=iOS' -derivedDataPath /Users/rob_dev/DocsLocal/motoguide/repo/DerivedData-SpeechCalibration-Followup-Release CODE_SIGNING_ALLOWED=NO` (expected: exit 0). Release resource exclusion: `find DerivedData-SpeechCalibration-Followup-Release/Build/Products/Release-iphoneos/RideHorizon.app -iname '*SpeechCalibration*' -o -iname '*calibration*'` (expected: no output). Release string exclusion: `strings DerivedData-SpeechCalibration-Followup-Release/Build/Products/Release-iphoneos/RideHorizon.app/RideHorizon | rg 'Speech Calibration|Use Candidate B for normal Premium Voice|RideHorizonInternalSpeechCalibrationRideOverride|RideHorizonInternalSpeechCalibrationProfiles|Candidate B active'` (expected: no output). Install: `xcrun devicectl device install app --device 00008150-000C70883E87401C /Users/rob_dev/DocsLocal/motoguide/repo/DerivedData-SpeechCalibration-Followup/Build/Products/Calibration-iphoneos/RideHorizon.app` (expected: installed bundle `ai.digitalmercenaries.ridehorizon`).
- **Physical calibration findings — 2026-08-04: REVISE.** The system output-volume value remained static when Rob used the iPhone hardware buttons; this physical result supersedes the model-level observer test as behavioural evidence and leaves live reporting unresolved. Audible distortion begins around `+12 dB` of additional output gain; retain `+15 dB` only as a deliberate internal rejection-boundary probe. Strong compression remains perceptually subtle. Candidate B at `+12 dB / Strong / +6 dB presence` sounds thinner yet remains softer than densely mastered rock music. Add `+9 dB` and `+15 dB` presence options only as later internal probes. Do not change the production profile from these observations alone.
- **Audio-calibration research — 2026-08-04:** `docs/research/2026-08-04-audio-speech-calibration-research.md` audits the implemented processing chain, explains the loudness/masking result, defines compressor and limiter measurements, and proposes safe stationary motorcycle-noise simulation. Candidate A applies variable bounded peak normalisation of 0 to +6.02 dB; Candidate B is not raw ElevenLabs audio and additionally uses a 100 Hz high-pass, presence EQ, compression, additional requested output gain and limiting. No code change, build, deployment or profile promotion is authorised by this research.
- **RH-004A — Diagnose live system-volume reporting: SHAPING.** Compare real `AVAudioSession.outputVolume` KVO with a visible system `MPVolumeView` on the physical iPhone across idle, YouTube Music, fixture playback and Bluetooth route changes. Do not keep the RideHorizon audio session active merely to update the display. Exit with a reliable live indicator or an accurately documented platform limitation.
- **RH-004B — Measure the speech-processing chain: SHAPING.** Produce repeatable per-fixture input/output true peak, active speech level, loudness, crest factor, compressor gain-reduction and limiter-engagement evidence. Use the measurements to explain the existing Off/Light/Medium/Strong separation before changing the compressor range.
- **RH-004C — Bound the internal controls: SHAPING.** Use additional-output-gain choices `0, +6, +9, +12 and +15 dB`, mark `+12 dB` as the observed distortion onset and `+15 dB` as a deliberate rejection-boundary probe, and add presence choices `+9 dB` and `+15 dB`. Make clear that this control follows the variable 0 to +6.02 dB automatic normalisation shared with Current A. Keep all controls internal-only and do not promote a production profile.
- **RH-004D — Widen the compression probe from evidence: SHAPING.** After RH-004B, test two deliberately separated stronger presets and distinguish loudness-matched compression from compression plus make-up gain. Reject presets that do not produce meaningfully different gain-reduction distributions or that introduce clipping, pumping or lost consonant onsets.
- **RH-004E — Add controlled stationary masking evidence: SHAPING.** Triage Rob’s existing helmet-camera/in-helmet-microphone tour recordings and prefer privacy-clean, unclipped, stable excerpts with provenance. Treat their spectrum and dynamics as shaped by the microphone, placement, wind protection, camera gain/processing and codec—not as calibrated at-ear SPL. Compare fixed speech, licensed music and noise at stated relative levels while keeping absolute headset volume safe; use a licence-verified CC0 fixture only if the owned material is unsuitable. Treat this as repeatable screening, not a substitute for a ride.
- **RH-004F — Repeat the human calibration gate: SHAPING.** After RH-004A–E, compare Current A and bounded candidates in controlled stationary masking, then on the real motorcycle. Rob selects, revises or rejects the profile; production remains unchanged until that decision.
- **RH-004G — Stabilise Premium Voice active-speech level: VERIFY (2026-08-04).** Production Premium Voice now replaces peak-only normalisation with a bounded per-announcement, window-gated active-speech RMS calculation inside the existing processor, followed by a conservative 90 Hz high-pass, +2 dB presence, Light compression and the existing headroom-preserving sample limiter. Compatible multi-file TTS chunks are concatenated before DSP so filter, compressor and limiter state remains continuous across the announcement. Candidate B controls and the proxy contract are unchanged, and the heuristic is not described as LUFS, P.56 or true peak. Focused synthetic, bundled-fixture and multi-chunk tests passed; the complete `RideHorizonCalibration` unit target passed on the iPhone 17 simulator; signed Debug and unsigned Release generic-iPhone builds passed. The supplied crash report was an XCTest out-of-range failure in an obsolete two-buffer assertion, not a production-app crash; the corrected whole-utterance regression passed. Signed Debug `0.12.4 (20260804.0246)` built, installed and launched on the physical iPhone. Stop for phone/headset listening on that exact post-tweak build rather than tuning further automatically.
- **Outcome:** RideHorizon activates audio only around its own announcements, never inserts a timer-based primary-audio delay, pauses active speech only for observable interruption/secondary-audio begin events, resumes immediately on the matching end event, restores other audio smoothly, and produces privacy-safe evidence for failures.
- **User/operator:** A rider hears intelligible announcements without stuck music suppression or a sudden post-session volume jump, and RideHorizon yields to navigation whenever iOS exposes the competing audio event.
- **Boundaries:** Add a privacy-safe diagnostic ring buffer to private-beta builds for ride/app/audio state, request-stage timing, coarse public network-path classification, output-volume snapshots, route changes, interruption and silence hints, and playback lifecycle. Retain at most 2,000 events, seven days or 1 MiB, whichever limit is reached first; persist it with `FileProtectionType.completeUntilFirstUserAuthentication` so locked-device background writes remain possible after first unlock; expose view, export and clear controls under Advanced with stationary-use copy. Verbose proxy logs remain Debug-only and off by default. Before a public-production build, add a build-time compilation switch that defaults diagnostics off and can omit the persistent recorder and UI from the binary. Activate the playback session immediately before speech; deactivate it after finish, cancellation and failure using notification to other audio apps; bind cleanup to End ride; calibrate Apple and Premium Voice playback against normal music/navigation conditions. Do not log coordinates, fact text, credentials, carrier identity, IP addresses, SIM/service identifiers or external audio content.
- **Non-goals:** Identifying external apps by name, controlling system volume, recording external audio, or guaranteeing arbitration that iOS does not expose.
- **Dependencies:** RH-003 cleanup boundary, AVAudioSession behaviour on the target iPhone and at least one Bluetooth helmet headset.
- **Risk:** High because an audio regression can distract a rider or create an unsafe volume transition.
- **Decision impact:** Preserve the existing user choice to interrupt music, but change its scope from app lifetime to announcement lifetime. Settings information architecture is deferred to RH-005.
- **Delivery Risk Cube movement:** Hold breadth fixed; increase implementation fidelity through OS-event diagnostics and production-quality depth through bounded audio ownership.
- **Behavioural evidence:** `TF-AUDIO-01` and `TF-AUDIO-02` pass on the physical development candidate before upload. Retain `TF-AUDIO-03` to `TF-AUDIO-06` and the end-of-ride tests as release evidence for the exact Internal TestFlight binary under RH-002.
- **Quality evidence:** Tests cover finish, cancellation, stale Apple callbacks, interruption, provider failure, activation/deactivation failure, route change and media reset. Tests also prove the diagnostic age, count and byte bounds, file protection, backup exclusion and persistence; physical evidence records output-volume snapshots and subjective intelligibility without claiming control of system volume.
- **Deterministic verification:** Complete `RideHorizonTests` target and signed physical-iPhone build pass. Exact commands must be recorded with the implementation evidence.
- **Independent evaluation:** Fresh review checks every audio-session activation has bounded deactivation and no error path leaks active state. Rob performs Bluetooth and navigation coexistence tests.
- **Stop condition:** Stop before release on any stuck interruption, failed music restoration, sudden volume increase, failure to yield to an iOS interruption or primary-audio hint, or missing cleanup path. Treat overlap for which iOS emitted no observable event as a residual platform risk and physical-test finding, not a deterministic failure of an impossible guarantee.
- **Gate:** Pass the pre-road physical checks, return to RH-001 for replacement archive/upload, then run the complete field evidence set on that exact Internal TestFlight binary before RH-002 external submission.
- **Implementation evidence — 2026-08-03:** Audio ownership now begins at actual Apple or Premium Voice playback rather than request preparation and deactivates with `notifyOthersOnDeactivation` on every finish/cancellation/end path. Increment 1 makes the policy explicit: the enabled setting uses a non-mixing session to interrupt music, while the disabled setting mixes. Playback cannot begin if activation fails; three failed deactivation attempts fall back to a non-suppressing mixing configuration. Request preparation and actual playback are separate interface states, and both Test Mode advance buttons call `advanceTestLocation()`. The persistent diagnostic chain now correlates place lookup, fact generation, text readiness, TTS request, audio readiness, session acquisition, playback, terminal release, waiting, restart, cancellation and supersession without text, coordinates or credentials. Automated tests prove correlated lookup and announcement chains, one resumable restart, no stale restart, active supersession, real-MP3 preparation, Premium completion release and terminal cleanup. All 168 physical-iPhone tests passed; signed candidate `0.12.3 (20260803.2155)` built, installed and launched. `TF-AUDIO-01` and `TF-AUDIO-02` remain a human stationary gate because intelligibility and restoration are perceptual.
- **First physical gate evidence — 2026-08-03:** The phone export shows the latest successful Premium Voice samples taking five to six seconds from text ready to playback: three to four seconds of bounded primary-audio wait, one to two seconds of Premium Voice generation/download, and zero to one second of playback preparation/session activation. This rejects the hypothesis that the delay is entirely OpenAI or ElevenLabs latency; the latest name/street samples did not invoke OpenAI. A direct live proxy sample took 1,058 ms for speech and measured −24.1 LUFS integrated with a −8.2 dBFS true peak. The revised app decodes off-main, applies one bounded gain across the whole utterance, correlates lookup-to-playback diagnostics, and has automated release coverage for Premium playback completion.
- **Loudness evidence — 2026-08-03:** A live sample from the configured proxy and voice returned in 1,058 ms and measured −24.1 LUFS integrated with a −8.2 dBFS true peak. This supports bounded local peak normalisation of at most +6.02 dB to a −2 dBFS sample-peak target. It does not support system-volume control, a cross-app fade claim or speculative server transcoding.
- **Diagnostic architecture decision — 2026-08-03:** Keep the local, privacy-minimised timing chain enabled for the private beta. A fact-backed Premium Voice announcement uses two app-to-proxy calls: `/v1/fact` around OpenAI, then `/v1/speech` around ElevenLabs; Names Only skips the fact call and Apple Voice skips the speech call. At fact and Premium Voice request start/result stages only, capture Network-framework path state, interface class, expensive/constrained flags and iOS 26 coarse link quality where available. Public iOS APIs do not expose cellular dBm or signal bars, and deprecated carrier identity is neither necessary nor permitted in the diagnostic contract. Client stage duration includes any retry/backoff but does not persist individual attempt counts or failure classes; use Debug-only client logs and optional server diagnostics for deeper investigation, and add persistent per-attempt events only if beta evidence requires them. Before public production, introduce a separate diagnostic build condition so the persistent recorder and UI can be compiled out; verbose HTTP/DNS logs remain Debug-only and off by default.
- **Timer-removal candidate — 2026-08-03:** Candidate `0.12.3 (20260803.2218)` removes the artificial primary-audio timer and Debug bypass from every mode. A preflight hint does not delay speech; genuine observed interruption/secondary-audio intervals defer or pause one correlated announcement and restart it immediately only after all overlapping intervals end. A media-services reset also releases a stranded pause and restarts the still-current announcement. Coarse network metadata is attached only to fact and Premium Voice request start/result stages, including terminal Premium Voice failure. All 172 physical-iPhone tests passed, signed Debug and Release builds succeeded, the Fly health endpoint returned `ok`, and the Debug candidate was installed and launched. Stop at the YouTube Music perceptual gate before Google Maps work, archive or upload.

## Completed verification increments

### RH-035 — Retain and expose Apple reverse-geocoder place metadata

- **Type:** Vertical slice.
- **Claimed by:** Codex on branch `feat/sub-locality-geodata`.
- **Goal:** Retain the Apple reverse-geocoder's supported place fields on device, so the rider and field-test evidence can distinguish the most specific returned place label from its wider locality.
- **Outcome:** When Apple supplies `subLocality`, RideHorizon uses it as the current town/place label while retaining and displaying the enclosing `locality`. The expanded Location panel exposes each available address-related Apple placemark field: name, house number, road, sub-locality, locality, sub-administrative area, administrative area, postcode, ISO country code, country, inland water, ocean, areas of interest, timezone and Apple region identifier.
- **User/operator:** Rob can see the actual Apple resolution at Hersham, Weybridge, Claygate, Surbiton, Seething Wells and sampled boundary crossings, without inferring it from a coarser label.
- **Boundaries:** On-device `Address` modelling and Location-screen display only. Retain existing road, town, county, region and country announcement controls; a sub-locality participates through the existing town level. Do not add an AONB, national-park or conservation-area classifier, query Apple for boundary polygons, collect a coordinate grid, or expand the fact-proxy request/OpenAPI contract.
- **Dependencies:** Apple `CLPlacemark` documentation and `docs/research/2026-08-05-apple-place-label-and-uk-boundary-testing.md`.
- **Risk:** Low to medium. Apple fields are optional and descriptive rather than an authoritative UK boundary ontology; values may differ by coordinate, device locale, map-data version and iOS version. Additional on-device metadata must not reach diagnostics or the proxy.
- **Decision impact:** No ADR. This is a reversible on-device model extension; a future deterministic UK-boundary source or proxy-contract extension needs separate shaping and privacy review.
- **Delivery Risk Cube movement:** Increases implementation fidelity and field-test observability for the current private-beta geographic-correctness uncertainty; functional breadth and external data commitments remain fixed.
- **Behavioural evidence:** A synthetic address with `subLocality` `Surbiton` and `locality` `Kingston upon Thames` presents `Surbiton` as the current town, retains both values, and exposes the Apple fields in the Location hierarchy. Existing addresses without a sub-locality retain their prior summary and announcement behaviour.
- **Quality evidence:** Unit tests cover sub-locality precedence, hierarchy display and metadata retention; a physical-device build installs when the phone is available. No additional field is added to the proxy payload or release diagnostic schema.
- **Deterministic verification:** `xcodebuild test -project RideHorizon.xcodeproj -scheme RideHorizon -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath DerivedData-SubLocality -only-testing:RideHorizonTests` — expected result: `** TEST SUCCEEDED **`.
- **Independent evaluation:** Fresh standards and specification review of `main...HEAD`, followed by the iOS build result.
- **Stop condition:** Stop and ask Rob if implementing a requested label would require a new external geodata provider, a proxy payload/API change, storage outside the device, or a new on-road interaction flow.
- **Implementation and verification evidence — 2026-08-05:** Commits `ad3ad0a`, `3ebdf92` and `fa92766` retain Apple placemark fields on device, use `subLocality` as the current place when available, retain the enclosing locality, display each supplied field in the expanded Location panel and preserve ordinary compact summaries. The full `RideHorizonTests` target passed on the iPhone 17 / iOS 26.3.1 simulator. The signed generic-iPhone Debug build using `DerivedData-SubLocality` reported `** BUILD SUCCEEDED **`. Tests cover the full placemark-field mapping seam, ordinary versus sub-locality summary output and Kingston → Surbiton town-level announcement behaviour. The fact-proxy request/OpenAPI contract and release diagnostic schema are unchanged.
- **Independent evaluation — 2026-08-05:** Separate standards and specification reviews of `main...HEAD` identified a duplicate-locality regression and missing placemark/announcement coverage; all findings were corrected and re-reviewed. No material scope creep or remaining code/spec issue was found.
- **Device status — 2026-08-05:** Xcode did not list Robert's iPhone, so no install or live Apple lookup ran. This does not block the code slice; collect real Apple responses after the phone reconnects.
- **Gate:** **DONE.** Continue the existing RH-002 TestFlight evidence gate. Treat actual labels and any AONB, national-park or conservation-area appearance as measured Apple responses, not a promise of this model.

## Shaping

### RH-002 — Internal TestFlight smoke and external beta submission

- **Type:** Release and operations increment.
- **Outcome:** The exact processed build passes a clean internal TestFlight smoke test and is submitted for external TestFlight review with accurate metadata.
- **User/operator:** Rob and the first 3–5 named private testers.
- **Boundaries:** Internal group, clean TestFlight install, onboarding, consent decline/grant, Test Mode, facts, Premium Voice, Apple fallback, privacy link, App Privacy publication check, completion of `docs/operations/testflight/TESTFLIGHT_FIELD_TEST_EVIDENCE.md`, external group and review submission. No public TestFlight link and no public App Store submission.
- **Dependencies:** RH-001 Done and the uploaded build in `Ready to Submit` or equivalent processed state.
- **Risk:** Medium because the final approval and App Store Connect declarations require Rob's legal and account-holder judgement.
- **Decision impact:** No ADR. App Store Connect declarations remain Rob's responsibility where legal confirmation is required.
- **Delivery Risk Cube movement:** Raise implementation fidelity to the actual TestFlight distribution path and production-quality depth through independent clean-install evidence.
- **Behavioural evidence:** A tester with no prior Keychain state can use the core experience without credentials. The exact Internal TestFlight binary passes all mandatory stationary, background, audio, network, inactivity and end-of-ride evidence before external submission.
- **Quality evidence:** The exact TestFlight binary, public privacy page and live backend agree with the saved App Store Connect declarations.
- **Deterministic verification:** App Store Connect displays the processed build without `Invalid Binary` or `Missing Compliance`; TestFlight installs and launches it on the target iPhone.
- **Independent evaluation:** Rob completes the human smoke test; Apple performs TestFlight App Review.
- **Stop condition:** Stop submission on any crash, credential prompt, broken policy link, unavailable AI path, inaccurate declaration or unresolved safety issue.
- **Gate:** Submit for external review, revise if rejected, or pause if real-device evidence is unsafe.

### RH-013 — Architecture refactor programme

- **Type:** Maintainability and change-safety programme.
- **Plan:** `docs/architecture/plans/ARCHITECTURE_REFACTOR_PLAN.md` owns the architectural direction, milestone order and gates. This ledger remains the sole status authority.
- **Dependency:** Finish or deliberately pause the current RH-001/RH-003/RH-004 release gate, commit the accepted work, and start from a clean branch based on current `main`. Do not refactor the actively changing release candidate in parallel.
- **RH-013A — Typed settings boundary: READY AFTER DEPENDENCY.** Preserve every existing key, default and migration behaviour while introducing a typed `RideSettings` snapshot, a narrow `RideSettingsStore` and the existing `UserDefaults` adapter. Move ride-setting persistence behind that boundary without changing the Settings UI, release behaviour or calibration-only isolation. Add characterisation tests before migration and stop after this increment.
- **RH-013A risk:** Medium. Persistence drift could silently change rider, privacy, audio or internal-build behaviour. Do not rename keys, broaden calibration state into Release, introduce a package/framework or redesign Settings.
- **RH-013A behavioural evidence:** Clean defaults match the current app; previously stored values round-trip unchanged; explicit Test Mode, AI consent, voice/audio, diagnostic and build-conditional calibration choices retain their present behaviour; launch, Start ride and End ride behaviour remain unchanged.
- **RH-013A quality evidence:** Production code has one persistence adapter for ride settings; consumers receive the narrow store or value they need; tests use isolated stores and do not depend on global standard defaults; the complete existing iOS suite and unsigned Release build pass.
- **RH-013A deterministic verification:** `xcodebuild test -project RideHorizon.xcodeproj -scheme RideHorizon -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath DerivedData-ArchitectureRefactor -only-testing:RideHorizonTests` — expected result: `** TEST SUCCEEDED **` with zero failures.
- **RH-013A deterministic verification:** `xcodebuild build -project RideHorizon.xcodeproj -scheme RideHorizon -configuration Release -destination 'generic/platform=iOS' -derivedDataPath DerivedData-ArchitectureRefactor-Release CODE_SIGNING_ALLOWED=NO` — expected result: `** BUILD SUCCEEDED **`.
- **RH-013A independent evaluation:** A fresh review context checks persistence compatibility, dependency direction, test quality, Release/Calibration separation and accidental product changes.
- **RH-013A stop condition:** Stop on a red baseline, dirty or stale base branch, required key migration, observable settings change, calibration content entering Release, unrelated release-candidate conflict or need for a new dependency framework. Do not begin RH-013B automatically.
- **RH-013A gate:** Mark RH-013A Done only after both deterministic commands and independent evaluation pass. Return the programme to Shaping before selecting RH-013B.
- **Later increments — SHAPING:** application composition root; ride-session orchestration; announcement coordination; executable app–proxy contract and session expiry; proxy authority/repository decomposition with PostgreSQL evidence; typed diagnostic/speech events; complete Swift concurrency checking and evidence-led Core Location policy. Detail only the next one to three increments after the preceding gate passes.

- **RH-005 — Rationalise Settings information architecture.** First stabilise the ride-session and audio concepts. Then keep frequent rider choices prominent, group voice/audio and privacy coherently, move tuning and developer controls behind Advanced, and usability-test the result while stationary. Do not mix this visual refactor into RH-003 or RH-004.
- **RH-006 — Add bounded shared content caches.** After the private-beta audio path is stable, add small in-memory proxy caches for sanitised facts and raw ElevenLabs speech audio. Keys must hash every effective generation input and a manual cache revision; personalised requests must not leak content across riders. Keep authentication and abuse limits on cache hits, distinguish delivered requests from upstream generations in metrics, and do not add Redis, a database table or a Fly Volume until measured reuse justifies persistent infrastructure.
- **RH-007 — Add bounded on-device speech caching and predictive prefetch.** Explore an iPhone speech-audio cache for replay and poor-connectivity resilience, then use recent accepted locations, heading and speed to predict only the next one or two plausible boundaries. Prefetch must be cancellable, discard stale branches, obey consent and quotas, avoid unbounded mobile-data use, and never delay the current announcement. Do not infer a route from a single noisy location vector.
- **RH-008 — Explore planned-route prefetch without becoming navigation.** Evaluate GPX import and explicit destination input as optional sources for a coarse route corridor, with a user-selected or automatically bounded look-ahead such as the next ten candidate announcements. Investigate whether supported iOS navigation handoff mechanisms expose useful destination or route context; assume explicit RideHorizon input is required unless platform evidence proves otherwise. Navigation apps continue to own turn-by-turn routing.
- **RH-009 — Consider rider-facing guidance-volume presets.** After helmet calibration establishes safe defaults, test a small Normal/Louder-style control rather than exposing LUFS, EQ or compressor parameters. Do not add this setting before one production profile is validated across stopped, urban and motorway-like conditions.
- **RH-010 — Rationalise onboarding and the initial idle screen.** Clean-install observations from 2026-08-03 are non-blocking for the first friends-and-family TestFlight beta. Add a reliable contrast treatment behind onboarding text and replace dense introductory prose with scannable points. Remove or repurpose the out-of-sequence “Welcome to Italy” photograph-caption page while retaining its Creative Commons asset and attribution. Give every onboarding navigation control consistent semantics; review whether AI consent should use a distinct decision treatment rather than occupying the normal Next-button position, and present the full privacy policy in an in-app sheet with an obvious Done action. On the initial idle screen, remove contradictory “Tap Start Ride” guidance, make the primary Start Ride action unambiguous, and replace empty-state phrases such as “No spoken phrase yet” and “Tap to repeat the phrase” with explanatory placeholders until an announcement exists. Show repeat/stop instructions only when speech exists and put the normal repeat action before the exceptional stop action. Consider a brief positive acknowledgement after End Ride that tracking has stopped. Keep branding below the system status/notch safe area, rationalise the in-app road mark with the Home Screen app icon, and define a beta-build presentation flag separate from Test Mode so beta builds can show the version prominently in Settings while exercising production-like Test Mode-off behaviour. Do not mix this polish work into RH-001/RH-002 unless a later clean-install test exposes a privacy, safety, crash or credential blocker.
- **RH-011 — Add and validate iPadOS support.** After the iPhone private-beta path is stable, make iPad a declared supported device family and treat iPadOS as an explicit product and QA target rather than assuming iPhone compatibility. Audit and adapt onboarding, Location, Log, Settings, sheets, maps and stationary controls across representative compact and regular iPad window sizes, portrait and landscape, including Split View and Stage Manager where supported. Verify clean install, location and notification permissions, Start ride/End ride, background location, Test Mode, facts, Apple Voice, Premium Voice, audio coexistence, diagnostics export, interruption recovery and Dynamic Type. Run automated tests on an iPad simulator, then complete a physical-iPad smoke test, including Bluetooth audio where available. Before any public App Store release that declares iPad support, validate the universal archive and provide the required iPad screenshots and accurate review metadata. Do not claim iPadOS support until the physical-device checks pass; iPad-specific route planning, Pencil, keyboard and pointer features are non-goals unless later justified.
- **RH-012 — Remove redundant Names Only place components.** Manual Test Mode evidence on 2026-08-03 produced “Gloucestershire. Lydney, Gloucestershire.” Keep meaningful boundary context but avoid repeating the same county or place component within one short announcement. Treat this as non-blocking wording quality for the first friends-and-family beta unless repetition becomes materially distracting during the road test.
- **RH-014 — Increase the manual map zoom increment.** The zoom-in and zoom-out buttons work, but the current `1.3×` step is too subtle. Increase the fixed step to roughly `1.6×`–`2×`, then check both directions on the target iPhone and retain the existing minimum/maximum zoom bounds. Keep this session-only and non-adjustable; the separate map label scale setting must remain unchanged.
- **RH-015 — Use useful stepped boundary-trigger delays.** Replace the linear `0–60 s` slider with a discrete picker using No delay, 5 s, 10 s, 15 s, 30 s, 1 min, 2 min, 5 min and 10 min. Retain the existing stored setting key and 10-second default; map any previously saved non-option value to the nearest available step. Verify that the selected cooldown still applies between boundary-triggered announcements without delaying manual repeat or speech already in progress.
- **RH-016 — Make map town labels larger and adjustable.** The current “Map label scale” changes RideHorizon sheet and overlay text; it does not change Apple MapKit’s rendered town names. Stop applying that setting to the sheet and rename or replace it so its purpose is accurate. MapKit’s public standard-style API controls elevation, emphasis, points of interest and traffic, but exposes no native label-font-size control ([Apple MapStyle documentation](https://developer.apple.com/documentation/mapkit/mapstyle/standard%28elevation%3Aemphasis%3Apointsofinterest%3Ashowstraffic%3A%29)). First confirm this limitation and whether per-app Dynamic Type affects native labels on the target iPhone. If it does not, prototype an adjustable SwiftUI `Annotation` layer for nearby town names, using the planned nearby-towns data source, collision/priority rules and zoom-dependent visibility; do not claim that all native Apple map labels can be resized. Compare legibility, duplication, clutter, update cost and attribution/data-licensing needs before selecting the custom layer or a map provider with styleable vector labels.
- **RH-017 — Replay the exact last Premium Voice audio without regenerating it.** Tapping Last spoken phrase currently re-enters the normal Premium Voice path and makes another ElevenLabs request. ElevenLabs documents that TTS is nondeterministic and that a seed is only best-effort ([ElevenLabs TTS API](https://elevenlabs.io/docs/api-reference/text-to-speech/convert)); therefore retain the successfully prepared speech audio for the last announcement and replay those exact bytes locally. Invalidate it when the announcement text, provider, voice/configuration revision or processing profile changes, and fall back to generation only when no valid retained audio exists. Integrate this small one-entry replay cache with RH-007’s later bounded on-device cache rather than creating a competing cache design; tests must prove repeat makes no network request and produces byte-identical source audio.
- **RH-018 — Make inactivity confirmation and automatic ride end visible outside the app.** Physical testing on 2026-08-04 confirmed that automatic End Ride works while RideHorizon is foregrounded or backgrounded, but the inactivity confirmation is visible only inside RideHorizon. Add an actionable local notification that asks whether to continue the ride when the app is not visible, while preserving the existing safe automatic-end deadline if the rider does not respond. Send a second local notification confirming that RideHorizon has stopped location tracking when inactivity ends the ride. Keep the foreground presentation coherent with the same state and wording, avoid encouraging interaction while moving, and test foreground, background and locked-phone paths with notification permission allowed and denied. This is deferred and does not authorise an implementation change in the current release batch.
- **RH-019 — Automate protected TestFlight uploads from GitHub Actions.** After the first private-beta build has been uploaded through the already proven local Xcode path, add a manually triggered release workflow that operates only from protected `main` after required tests pass. Use a protected GitHub `testflight` environment and least-privilege App Store Connect API authentication; import Apple Distribution signing material into a temporary runner Keychain, archive and validate the Release app, upload a normal TestFlight-and-App-Store build rather than an Internal-Only build, retain useful non-secret delivery evidence, and verify Apple processing status. Keep build selection, legal/export-compliance confirmation, TestFlight groups and the first external-review submission manual until repetition justifies further automation. Preserve local Xcode upload as the documented recovery path. This is deferred and does not authorise workflow, credential or signing changes during the current release.
- **RH-020 — Split the public API hostname and retire website compatibility routing: SHAPING.** Adopt `api.ridehorizon.digitalmercenaries.ai` as the intended stable iOS API hostname and eventually make `ridehorizon.digitalmercenaries.ai` a fully static website. Follow `docs/architecture/plans/PUBLIC_SITE_HOST_AND_CI_PLAN.md`: first record the boundary decision in an ADR, inventory uploaded and distributed builds, configure and verify the API subdomain against the current Fly origin, update the iOS/OpenAPI/operational contract together, and prove a replacement Internal TestFlight build. Keep the existing Cloudflare pass-through until no supported build depends on the shared hostname; only then restructure the static paths, move headers to static configuration, remove `_worker.js`, its Node tests and the repository-owned Wrangler/npm deployment layer where the selected static deployment method permits. Risk is high because premature removal breaks AI facts and Premium Voice in existing builds. Stop before DNS, certificate, secret, production or App Store changes without the required authority and evidence.
- **RH-021 — Add minimal public-site CI and post-deployment quality reporting: SHAPING.** Implement the 80/20 stack in `docs/architecture/plans/PUBLIC_SITE_HOST_AND_CI_PLAN.md`: retain the existing Worker tests only while compatibility routing exists, deploy from protected `main` through one serialised GitHub production workflow, run Lighthouse CI on the three public HTML pages, run Lychee over the site and explicit public service files, and retain native JSON/HTML/JUnit evidence. The first integrated Lighthouse/Lychee run captures the baseline without hiding or pre-fixing known findings; only existing tests, deployment success and catastrophic public-page availability block this setup increment. Keep performance advisory. Use failed deployment status, notification and reviewed fix-forward by default, with manual Cloudflare rollback for an obviously broken release. Do not add a persistent staging site, redundant scanners, automatic LLM fixes or automatic rollback in this increment.
- **RH-022 — Remediate public-site CI findings and activate stable gates: SHAPING AFTER RH-021.** Preserve the first complete Lighthouse CI and Lychee report set as the baseline, map deterministic findings to their owning files, fix hard public-file, link, metadata, accessibility, SEO and stable best-practice failures, and re-run the same checks. Treat performance separately and change code only for reproducible causes. After correction, promote agreed stable assertions to blocking gates. An LLM may consume the structured reports and prepare a reviewed fix-forward change, but the standard tools remain authoritative and no threshold may be lowered or finding suppressed merely to obtain green CI. Finish with zero unresolved hard failures, explicit treatment of advisory findings and retained before/after evidence.
- **RH-026 — Detect and announce meaningful landscape-region boundaries.** Extend the deferred Milestone 3 deterministic boundary layer beyond administrative areas so RideHorizon can say “You are entering the Peak District”, “Welcome to the Cotswolds” or “You are now in the Low Weald”. Treat statutory National Parks and National Landscapes/AONBs as authoritative designated polygons, using current government boundary data where licensing and attribution permit ([Natural England National Parks data](https://www.data.gov.uk/dataset/334e1b27-e193-4ef5-b14e-696b58bb7e95/national-parks-england1), [Natural England AONB data](https://www.data.gov.uk/dataset/8e3ae3b9-a827-47f1-b025-f08527a4e84e/areas-of-outstanding-natural-beauty-england1)). Model broader physical or cultural regions such as the Low Weald separately because their boundaries and authority differ; use an authoritative landscape-character dataset where one exists and explicitly curate or omit ambiguous regions rather than asking an LLM to infer membership. Support overlapping and nested regions, boundary hysteresis for GPS jitter, entry/exit deduplication, an independent “Landscape regions” announcement toggle, and priority/cooldown integration so a landscape announcement does not create a burst alongside town or county speech. Use the current public name “National Landscape” while retaining AONB in data provenance and legal metadata. Start with Gloucestershire test coverage, including the Cotswolds boundary, then validate representative National Park, National Landscape and landscape-character-region crossings before wider UK rollout.
- **RH-027 — Add sparse narrative ride recaps.** From time to time, summarise the ride so far to reinforce geographic context and the story of the journey: places and regions crossed, meaningful landscape changes, contrasts and recurring themes. Build this on RH-024’s bounded ride context and RH-026’s landscape regions, using only verified places, regions and facts already associated with the current ride; do not invent route details or send an unbounded coordinate history. Prefer event-based eligibility after several meaningful announcements or entry into a materially different region, with a generous minimum time and distance since the previous recap, rather than a simple frequent timer. Generate nothing when there is too little new material. Keep the spoken recap to one or two short sentences, make it independently controllable, queue it below new boundary announcements, and deliver it only while stopped or during a future verified low-demand riding window. Offer an end-of-ride recap separately from automatic mid-ride recaps. Reset transient recap state at End ride and define explicit retention/consent before saving any journey narrative beyond the active ride. Tests must prove trigger spacing, factual grounding, deduplication, boundary priority, cancellation and no recap after End ride.
- **RH-024 — Make ride fact generation sequence-aware without destroying useful caching: SHAPING.** Test Mode and normal riding currently use the same fact-generation path and the same 30-day on-device `PlaceFactCache`; neither `PlaceFactRequest`, its cache key nor the proxy/OpenAI prompt contains previous generated or spoken facts. Re-entering a place with identical place, mode and rider-preference inputs therefore returns the exact cached fact, while changing a preference changes the key and generates a different result. The key also lacks a prompt/cache revision, so a server-side prompt change does not invalidate already cached responses. **Tier 1:** retain a small local same-day memory; suppress immediate boundary bounce; use a deterministic “Back in …” acknowledgement instead of replaying an identical cached fact; on genuine cache misses, send at most the last ten announcement facts plus the last two facts for the same normalised place as bounded exclusion context; add a cache/prompt revision. Do not put rolling history into the main cache key. **Tier 2, only if road evidence justifies it:** store two or three bounded variants per stable place/preferences key, select an unused variant locally, and add a concise revisit-summary depth rather than replaying Long Facts. Defer embeddings, vector search, server-side rider history and semantic classifiers. Tests must cover Test Mode/normal parity, immediate bounce, same-day return, changed preferences, prompt revision and nearby-place thematic repetition.
- **RH-025 — Rationalise fact-prompt composition and override semantics: SHAPING.** The proxy currently appends three substantially overlapping policy layers: `BASE_SYSTEM_PROMPT`, `FactMode.defaultPrompt()` and one first-match `Additional mode prompt`. It also hard-codes geographic/cultural context as roughly 70% of every fact, overriding the rider’s selected interests and making POIs unlikely to surface within a short announcement. Remove that fixed percentage and let explicit category selections and custom focus determine emphasis, while keeping safety, factual grounding and length fixed; prefer simple category choices over numerical weights unless testing shows riders need them. The fact request also omits the deterministic base announcement text, so the model cannot reliably avoid repeating place components that the app subsequently prepends. **Tier 1:** reduce this to clearly named composable sections—fixed safety/factuality, output depth, visit/novelty policy, rider interests and place context—while preserving current external behaviour except for removal of the fixed 70% weighting; pass the already-known base announcement or conveyed place components so the fact does not repeat them; correct the `nation` hierarchy lookup; and either remove the unreachable `X-RideHorizon-User-Id` override path or explicitly defer it. Keep runtime tuning to one documented global mode/emphasis source and snapshot-test the final OpenAI messages, including selected-interest behaviour. **Tier 2, only if quality experiments require it:** restore narrowly scoped boundary or place-specific emphasis with explicit inheritance rules. Do not build a general prompt-management system. Preserve prompt-injection boundaries, bounded override length, last-known-good refresh behaviour and the narrow app-to-proxy contract.
- **RH-028 — Evaluate selective web-grounded place facts: SHAPING AFTER RH-024/RH-025.** The current `gpt-4o-mini` Chat Completions call has no search tool and relies on model pre-training. **Tier 1:** run a bounded proxy spike using OpenAI Responses `web_search` on a representative set of ordinary places, civic-history quirks and precise administrative claims; compare factual usefulness, citations, latency and cost against the existing path, with no iOS or production change. Test a low search-context setting and a small authoritative-domain policy, and determine whether automatic search is selective enough. **Tier 2, only if the spike meets rider-latency and quality thresholds:** enable search server-side only for cache misses and claims needing precise or current grounding, retain source metadata with the cached fact, and expose compact clickable sources in the app Log without speaking URLs. Keep ordinary stable facts on the faster non-search path. Defer a bespoke Wikipedia/Wikidata ingestion pipeline, vector database, broad crawling and deep research unless hosted search proves inadequate.
- **RH-029 — Implement the approved RH-D visual identity across product surfaces.** Use the approved source and derived assets in `resources/brand/ridehorizon-rh-d-2026-08-04/` as the implementation source of truth. Replace the temporary iOS app icon and reconcile in-app branding without changing product behaviour; add the appropriate light/dark web lockups, SVG favicon, PNG/ICO fallbacks, Apple touch icon and web-app manifest icons to the public site; preserve the mathematical diagonal colour plane, palette and clear-space rules rather than recreating the mark by eye. Verify the app icon at Home Screen, Settings, notification and App Store sizes on the physical iPhone in default, dark, clear and tinted appearances; verify favicons at 16, 32 and 48 px and maskable icons against their safe area; run the existing iOS and public-site checks; and update App Store or deployed website assets only with explicit release authority. The current asset-pack task does not authorise application, Xcode-project, website, deployment or App Store changes.
- **RH-030 — Discover motorcycle-specific points of interest.** Extend the generic MVP2 POI work with rider-specific categories such as motorcycle cafés and meeting places, dealers, repair and tyre shops, motorcycle museums, and other genuinely useful touring stops. Start with a data-source and coverage spike: evaluate explicit OpenStreetMap motorcycle tags and a compliant Overpass or hosted extract for structured facilities ([OpenStreetMap motorcycle tagging](https://wiki.openstreetmap.org/wiki/Motorcycle_namespace)), then compare Apple/Google place search, manufacturer/dealer directories and curated motorcycle-community sources for categories such as bike cafés that lack consistent canonical tagging. Do not classify an ordinary café as motorcycle-specific from its name or generated text alone. Preserve source, licence, freshness and verification status; deduplicate providers and provide a source/deep link. Rank sparsely by rider relevance, trustworthy opening status, distance and reasonable route-corridor fit rather than raw radius alone. Keep suggestions independently controllable, suppress repeats, and hand accepted destinations to the later POI/navigation flow instead of calculating routes.
- **RH-031 — Discover timely nearby events.** Explore sparse announcements for events happening today or soon within a rider-selected radius and time window, covering both motorcycle/motorsport events and distinctive general local events. There is no single canonical feed: test authoritative sport calendars such as ACU’s Great Britain programme and FIM calendars ([ACU](https://www.acu.org.uk/), [FIM](https://calendar.fim.ch/)), a licensed aggregator such as Ticketmaster Discovery—which supports location, radius, classification and date filters but will not cover every local event ([Ticketmaster Discovery API](https://developer.ticketmaster.com/products-and-docs/apis/discovery-manual/v2/))—and structured council, tourism or organiser feeds for local festivals and traditions. Do not scrape sources whose terms do not permit it, infer that an annual event is happening this year, or announce stale or cancelled listings. Define provenance, update and cancellation handling, timezone-safe dates, deduplication, confidence, radius, look-ahead and relevance rules before implementation. Keep event suggestions optional, brief and subordinate to boundary speech; favour holiday/touring mode, stopped or verified low-demand delivery, and include a source/deep link for later inspection. Validate source coverage in one UK test region and one European touring region before choosing providers or making a general-events promise.
- **RH-033 — Add beta AI-capacity monitoring and graceful degradation: NFR / SHAPING.** Prevent exhausted OpenAI or ElevenLabs credit, provider-account capacity and related upstream failures from becoming unexplained beta failures. Preserve the existing safe service behaviour—fact-generation failure falls back to the base place announcement, and Premium Voice failure falls back to Apple Voice—but make the degradation visible outside Test Mode with brief non-technical copy, a stable reportable error code and timestamp in diagnostics; never expose provider billing details, credentials or raw upstream bodies to testers. Treat capacity, authentication/configuration, throttling, timeout and provider `5xx` as distinct internal categories: the current proxy maps provider failures to its own contract (including HTTP `502` and `RH-TTS-02` for Premium Voice account capacity), so do not assume a provider `412` is the client-facing signal. Add operator-only counters, last-success state and alerts, with immediate notification for capacity/authentication failures and threshold or consecutive-failure alerts for transient errors. Reconcile the proxy’s existing request/character quotas with provider data: ElevenLabs exposes subscription usage and limits through `GET /v1/user/subscription`, while OpenAI exposes organisation usage and cost endpoints requiring an admin key ([ElevenLabs subscription API](https://elevenlabs.io/docs/api-reference/user/subscription/get), [OpenAI Usage API](https://platform.openai.com/docs/api-reference/usage/audio_transcriptions_object)); keep any provider balance/usage view behind authenticated administration rather than the public liveness endpoint. Prefer passive polling plus telemetry from real requests; evaluate a tagged, budget-capped synthetic fact/TTS canary only if an end-to-end signal is still needed, exclude it from tester quotas, and do not rely on it as the only alert. Provide a small operator dashboard or equivalent report showing daily requests, speech characters, provider usage/cost, quota headroom and recent categorised failures, with configurable warning/critical thresholds. OpenAI project budgets are soft alerts rather than hard stops, so retain proxy-side beta limits and limited prepaid funds as separate cost controls ([OpenAI project budgets](https://help.openai.com/en/articles/9186755-managing-projects-in-the-api-platform)). Acceptance evidence must simulate exhausted OpenAI and ElevenLabs accounts, prove the correct in-app fallback and report code, prove operator alerts and usage reporting, and prove that public health responses and logs disclose no secrets or account details.
- Full App Attest enforcement after the first private beta.
- Public App Store metadata, screenshots, support page, regional declarations and launch hardening.

## Family-trip validation priority overlay — 2026-08-10

Purpose: maximise learning from Nishant's current European family trip while the reference use case is available. The immediate reference session is two siblings, roughly ages 8–9 and 14–15, sharing one iPad and listening through the vehicle speakers. This is a temporary product-priority overlay for a family/kids derivative that shares RideHorizon capabilities; it does not cancel the RideHorizon motorcycle release gate, prescribe the technical product split, or authorise unrelated architecture work.

Priority semantics: **High** means directly enables the live trip or tests a key product hypothesis and should be attempted before general polish; **Medium** means valuable once the High slice works but should not delay field learning; **Low** means retain the idea without spending Nishant-trip time on it.

### High — maximise the live-trip learning window

- **RH-011 — iPadOS support: HIGH, narrow family-trip slice.** Pull forward only what is needed for the shared back-seat test: use the full iPad window cleanly in portrait and landscape, reproduce and fix the iPad-only failure to select Short Facts and Long Facts, verify the core map/location controls, and prove spoken output continues through the iPad/car-speaker route while the app is backgrounded or the screen is locked where platform policy permits. Complete a physical-iPad smoke test. Do not block this slice on Split View, Stage Manager, App Store screenshots, universal-release polish, Pencil, keyboard or pointer support; those remain in the broader RH-011 scope.
- **RH-034 — Add a shared mixed-age family session profile: HIGH.** Support one journey session shared by two children of materially different ages, initially the 8–9 and 14–15 reference case. Capture simple age/maturity bands and a small set of interests; let the narration balance both children rather than averaging them into generic “kids” copy; use natural British English and avoid patronising the teenager or over-complicating material for the younger child. Allow simple voice or UI adjustments such as more/less detail and more/less of a topic. Keep the first slice session-scoped and privacy-minimised; no account system, biometric speaker identity or durable child dossier is required.
- **RH-046 — Add bounded spoken contextual follow-up questions: HIGH.** Bring forward the first part of the interactive-guide direction for the family product. Use explicit, bounded listening rather than an always-on microphone; resolve questions against current best-available location, the last announcement/subject and a small recent-journey context; support at least “what is that?”, “why?”, “tell me more”, “repeat”, “shorter” and stop/cancel. Answers should be brief by default and age-appropriate for the shared session, with uncertainty stated rather than guessed. Keep the role bounded to the journey, places and suitable general learning; safely redirect inappropriate or unrelated use rather than becoming an unrestricted general-purpose chatbot.
- **RH-036 — Evaluate and announce interesting moments beyond boundaries: HIGH.** Keep the existing frequent location sampling; treat a town/county/country boundary as only one reason to consider speaking. Add an eligibility/evaluation layer that may be triggered by meaningful boundary change, elapsed quiet time, distance moved, destination progress or nearby natural, geographic, cultural, historical or infrastructure features. Use deterministic rules to decide when evaluation is warranted and hard cadence limits; let the content-selection layer decide whether anything currently earns attention, including a positive decision to remain silent. Avoid a universal fixed one- or two-minute cadence: dense Paris and sparse countryside should naturally produce different opportunities. Prefer observable “look left/right/ahead” or landscape-context moments where confidence is sufficient, and drop stale moments rather than playing a backlog. This does not authorise route planning.
- **RH-047 — Make UK/European pronunciation and local-language handling a first-class quality target: HIGH.** Test narration as a knowledgeable British-English guide would speak it: use conventional English exonyms where appropriate, pronounce retained foreign names recognisably and respectfully inside an English sentence, and avoid naive spelling-to-sound output or exaggerated accent switching. Build a small European route corpus containing difficult place, mountain, river and region names and include British names that expose naive TTS failures. Where useful for children, optionally give the local form, a slowed/syllabified repeat, a short etymology or a basic local greeting. Acceptance is human intelligibility/naturalness with British listeners, not merely generated IPA or provider claims.
- **RH-038 — Run comparative family-trip field validation: HIGH / VALIDATION.** Use Nishant's trip as a comparative study rather than waiting for feature parity. Have the family rotate through the prototype and the closest available competitors such as Roadguide and LoreRoad, adding Wexplo or Drive Stories when practical. Capture after each session: whether content matched the actual surroundings; younger-child engagement; whether the teenager found it childish; spontaneous questions/follow-ups; whether it caused them to look outside; too much/too little talking; repetition; pronunciation failures; incorrect or overconfident claims; requests the product could not satisfy; and whether they would choose it again over normal back-seat entertainment. Preserve concise evidence rather than relying on memory.
- **RH-040 — Run a family-product demand smoke test: HIGH / VALIDATION.** In parallel with the prototype, put up the simplest credible landing-page/signup test for the family proposition rather than treating one enthusiastic friend as product-market fit. Test a small number of distinct propositions such as “less screen time, more looking out of the window”, “an interactive family guide for European road trips” and “answers children's questions about the places they are actually travelling through”. Capture email/beta intent and basic family/travel context; do not wait for competitive feature parity before testing demand.

### Medium — add after the High slice is usable

- **RH-024 — Sequence-aware journey memory: MEDIUM, pull Tier 1 forward for the family variant.** Retain a bounded active-trip memory of recent places, subjects, questions and already-used facts so nearby announcements do not repeat the same regional setup and follow-up answers can make useful callbacks. Keep it bounded and trip-scoped first; do not block the field test on embeddings, vector search, server-side history or permanent profiles.
- **RH-008 — Destination context without navigation: MEDIUM.** Pull forward a narrow explicit-destination input so the family guide can understand the broad journey arc, give coarse progress/orientation and anticipate destination-relevant context. A searched or otherwise explicitly supplied destination is enough for the first slice. Do not build turn-by-turn routing, infer a precise ETA from straight-line distance, or require integration with the driver's navigation app before testing value.
- **RH-027 — Family journey recap: MEDIUM.** Adapt the existing recap idea to a passenger context: end-of-journey summaries can mention places/regions crossed, recurring themes and interesting questions; a later multi-day version may briefly recall the previous day when useful. Keep recaps factual and based on recorded journey context rather than inventing a polished story after the fact.
- **RH-039 — Add a parent-visible journey and curiosity summary: MEDIUM.** Explore a lightweight parent view or exported recap containing the route/places encountered, topics discussed, children's questions and session preference changes. Treat it as a family memory and product-feedback surface, not educational scoring, surveillance or a behavioural dashboard. Do not require a separate parent account for the first experiment.

### Low — retain, but do not spend the current trip on them

- **RH-041 — Durable multi-day family profiles and preference memory: LOW.** Later decide what age, interests, preferred detail, vocabulary level and likes/dislikes should survive beyond the active trip, with explicit parent visibility and retention controls. The current experiment can keep these session- or trip-scoped.
- **RH-042 — Coordinate multi-device/shared-headphone family sessions: LOW.** A later version may synchronise common announcements across devices while allowing individual questions or age-specific detail through separate headphones. Do not design distributed session coordination before the single shared-iPad/speaker experience proves useful.
- **RH-043 — Curriculum alignment, quizzes and gamification: LOW.** Preserve the option to connect real-world observations to KS2/KS3/GCSE concepts, lightweight quizzes, missions or recall prompts, but keep the initial product as curiosity-led travel rather than tutoring. Add explicit curriculum/game mechanics only if family evidence shows they increase engagement without making the experience feel like schoolwork.

### Existing work not on the Nishant critical path

Do not block this family-trip slice on the RH-013 architecture refactor, full RH-011 iPad release breadth, RH-014/RH-016 rider-map polish, RH-019–RH-022 public-site/release automation, RH-026 deterministic UK landscape polygons, RH-030 motorcycle-specific POIs or RH-031 broad event discovery. RH-025 prompt rationalisation can support the family prompt/profile work where necessary, but the field experiment should not wait for a general prompt-management system. RH-033 capacity/degradation work becomes important before wider beta recruitment, not before the first controlled family test.
