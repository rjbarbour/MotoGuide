---
id: RH-029.01
title: Apply the approved RH-D icon to the iOS app
status: In Progress
assignee:
  - '@codex'
created_date: '2026-08-21 11:03'
updated_date: '2026-08-21 11:06'
labels:
  - brand
  - ios
dependencies: []
references:
  - resources/brand/ridehorizon-rh-d-2026-08-04/README.md
modified_files:
  - RideHorizon/Assets.xcassets/AppIcon.appiconset
parent_task_id: RH-029
priority: high
type: feature
ordinal: 213000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Replace the temporary RideHorizon iOS app icon with the owner-confirmed approved RH-D v6 app-icon assets. This child item covers only the iOS app icon; website lockups, website deployment, App Store metadata and TestFlight upload remain outside this pull request.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The production RideHorizon target uses the approved RH-D v6 app icon from resources/brand/ridehorizon-rh-d-2026-08-04 without redrawing the mark.
- [x] #2 The integrated icon preserves the approved rendered pixels, is 1024 by 1024, has no alpha channel and passes Xcode asset-catalogue compilation.
- [ ] #3 A signed iPhoneOS build compiles with the replacement icon and the pull request is merged to main.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create the scoped branch from clean current main. 2. Replace the production AppIcon asset catalogue with the approved staged RH-D iOS asset set. 3. Run brand-pack validation, asset integrity checks and one signed physical-device build. 4. Commit, verify the GitHub credential profile, push, open and merge one pull request. 5. Verify merged main and update the child work item with evidence.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implementation replaces the temporary multi-PNG icon catalogue with the approved RH-D v6 universal 1024 px asset and its staged catalogue metadata. The approved fully opaque RGBA raster was flattened to RGB without changing visible pixels so the production App Store icon contains no alpha channel. Exact-device build was unavailable because the iPhone disconnected; the signed generic iPhoneOS build compiles the app, embedded Live Activity extension and asset catalogue successfully. The pack-wide validator is currently blocked by a pre-existing stale README.md hash in manifest.json; this child task does not regenerate the distributable brand pack.

Verification: approved-source versus integrated RGB pixel comparison passed; production icon is 1024 x 1024 RGB with no alpha; Contents.json parses with jq; git diff --check passed; signed generic iPhoneOS Debug build completed with BUILD SUCCEEDED, including actool compilation, the main app and embedded Live Activity extension.
<!-- SECTION:NOTES:END -->
